# Current Session State — 2026-08-30

Snapshot of exactly where things stand. Read [GAME_DESIGN.md](GAME_DESIGN.md) first for the actual mechanics — this file is about *status*: what's committed, what's still open, and what to do next.

The previous snapshot (2026-08-22, the Arachnid+ tier work and the two balance retunes) is preserved in git: `git show cc432e8:CURRENT.md`.

## Headline: the six Symbiote decisions are closed, **rounds two through eight are now committed and pushed** — round eight, the first real-money sale in the game, went up as `d91a092` — and **round nine, the seven free community-server perks, is written, verified, live-tested, and revised once off that test, but still not committed.** `main` and `origin/main` are both at `d91a092`, fully in sync.

**This file has now been stale about its own commit state twice, in the same field, and it warned about it the first time.** On 2026-08-28 it claimed rounds two–seven were uncommitted; they weren't. It then claimed round eight was uncommitted across 6 files — also true when written, also false now (`d91a092`, 8 files including both docs). Verified 2026-08-30 by `git rev-list --count origin/main..main` (0), `git rev-list --count main..origin/main` (0), and `git show HEAD --stat`. **Treat every commit/push claim in this file as needing re-verification, not reading** — they're the parts that rot fastest, and the working tree is the only thing that actually answers them. (Those commits landed under informal messages — `AAAAAAAAAAAAAAAAAA`, `fix issue`, `added emojis`, `add web fluid shop` — rather than the per-round split this file recommended, so `git log` is not a reliable index of which round is which. The round write-ups below are.)

Nine rounds of work. The first closed six problems surfaced by a review of the Symbiote tier (table below). The second came from the owner playing the result and reporting five things — branding, a broken-inventory report, camera icons, and revocation. The third gave the tiers a *visible* perk (a Patreon-coloured accent bar on every Components V2 panel) and closed the last hole in the refresh loop's notification path. The fourth is seven more reports against the Symbiote tier, and it was the largest batch — it deleted a mechanic, widened another, retuned a third, added a new service, and reskinned a whole command. The fifth reopened three of round four's own answers: a camera that still wouldn't retire, Venom Blast turned from an automatic save into a **button the player presses**, and the override rate doubled again to 0.30. The sixth was one question — *"if Stealth Mode triggers, does the embed show the stealth emoji and the Symbiote branding?"* — which turned out to be half yes, half a perk with no surface at all, plus one instructed number, Biomorphic Webbing's ambient-scavenge rate at **0.30**. The seventh finished that thought at the owner's instruction: Stealth Mode now **DMs its owner** when it turns an attempt away, so the perk has a push surface and not just the card.

The eighth is a different kind of change from the seven before it — the first thing in the game **sold for real money**, off-platform on the Patreon shop and fulfilled by hand, plus the raw-user-ID admin grant that makes fulfilment possible at all. It went up as `d91a092` across 8 files.

The ninth is the eighth's exact opposite: **seven perks that cost nothing**, paid for in Discord boosts and server activity rather than money, gated on three role IDs read from `.env` in one guild. It is the only uncommitted batch, and it is the largest in the file — **23 modified code files plus 2 new ones** (`services/server_perks.py`, `cogs/perks_cog.py`), plus both docs. Its headline is not a mechanic at all: **the blocker this track sat behind for nine days was false**, and two lines of `discord/member.py` were enough to disprove it. It then got a second pass the same day, after the owner live-tested it and reported seven notes — six copy and glyph swaps, and one that reversed a design rule the first pass had written into a docstring as a requirement.

The single most important thing in the second round: **#2's refresh loop detected lapses but nothing read the result.** Two perks were gated only at purchase, so a cancelled or unlinked pledge kept them working forever. That's now closed at use time.

The single most important thing in the third: **the refresh loop detected *upgrades* and nothing read those either.** Someone who linked their Patreon before pledging got the "nothing active yet" welcome, and when the pledge landed the background job switched their perks on in total silence.

The single most important thing in the fourth: **three of the seven perks reported as "not implemented" were implemented and firing.** The override was hitting 9.94% of Evade presses, Venom Blast 3.4% of the only boss a fresh account reaches, Stealth Mode every idle victim — all invisible, unattributed, or scoped to a surface nobody visits. *When a paid perk is reported missing, measure its fire rate and check its attribution before touching its logic.*

The single most important thing in the fifth, and the one worth carrying forward: **round four's camera fix passed its own check script and still didn't fix the report.** `check_camera_gold.py` asserted "Silver ends up unequipped, Gold ends up equipped" — true, and shipped, and the owner still saw the Silver camera in `/inventory` the next day, because *unequipped is not gone* and nothing on that surface said otherwise. The assertion encoded the intended design faithfully; the intended design was the bug. *A passing check proves the code matches the design. It never proves the design matches the report — for that, go look at the surface the reporter was looking at.*

The single most important thing in the sixth: **a perk can be fully implemented, correctly attributed, under test — and still have no surface its owner will ever see.** Stealth Mode fires only while the target has been idle 20+ minutes, and it renders a panel for the **thief**. "It worked" and "you weren't watching" are the same condition, so the subscriber paying for it had no way to learn it had ever done anything. *When auditing a perk's visibility, ask who is holding the phone when it fires — §9's attribution rule only governs the message that gets sent, not whether anyone with a stake in it is on the receiving end.*

The single most important thing in the seventh: **the throttle a notification needs is the most likely place to reintroduce the bug the notification was fixing.** Stealth Mode's DM has to be rate-limited — the perk fires while its owner is away, so one absence can stack a dozen identical messages. But the throttle is a `cooldowns` row on the *target*, and if an ordinary shakedown had been allowed to consume that window, the target would then miss the next attempt the perk actually turned away, which is the only thing the DM exists to report. *When you add a suppression mechanism to a notification, write the test that proves the suppressed-for-the-wrong-reason case is impossible — the happy path and the throttled path both look correct without it.*

The single most important thing in the ninth, and the one that generalises furthest beyond this repo: **a blocker recorded in a design doc is a claim someone made once, not a fact the code enforces.** §9.5 said this whole track "requires re-enabling the Discord Members intent", so it read as a live-bot config decision rather than code and went unbuilt for nine days. It was wrong — Discord ships the invoker's roles and boost timestamp inside every interaction payload, and `discord/member.py:349` and `:346` store both verbatim, no intent involved. The owner challenged the line and the owner was right. Second, smaller, and from the same round: **writing the documentation is what found the only bug in it.** A sentence I was about to commit claimed `_webbing_note()` needed no third branch; checking it against `patrol_cog.py:145` showed a booster with no pledge was being credited to **Arachnid**, a paid tier they don't hold. No behavioural test could have caught it, because Organic Webbing's *effect* is byte-identical from both routes — only the badge was lying. *When a perk becomes reachable from a second source, the behaviour is the easy half; go read what the card now claims about **why** it fired.* Third, from the live-test pass: **the mechanism you're proudest of is the thing most likely to leak into player-facing copy.** Two sentences got cut — *"it switches on and off with them — nothing to claim and nothing to choose"* and *"it's handed to you automatically, nothing to claim"* — both true, both describing the implementation's most interesting property, neither of any use to someone who never knew a claim step was possible.

### Round one — the six decisions

The session opened with a review of the Symbiote tier that surfaced six problems; the owner was given options for each and picked one per problem ("okay present me with options to how you recommend we deal with each problem, ill give you the answer"). Every one of them has shipped.

| # | Problem | Decision | Commit |
|---|---|---|---|
| 1 | No Symbiote purchase gate existed — `ARACHNID_GATED_ITEM_KEYS` was one flat set with a single rank check | **Per-key rank map** | `faed3d0` |
| 2 | A lapsed pledge kept every perk forever — tier was written at link time and never re-read | **Background refresh loop** | `cd06e3a` |
| 3 | Symbiote had no always-on cost, only a single boss drawback | **"The suit overrides you in combat"** | `49ad4f9` |
| 4 | Venom Blast's `2x` was never validated | **Validate it** | `49ad4f9` |
| 5 | Stealth Mode's 20 minutes was never measured | **Validate it** | `5c41181` |
| 6 | Biomorphic Webbing promised 3 rolls; 2 are combat-only | **Add the "(combat patrols)" qualifier** | `635c104` |

Ten code commits landed in round one (plus `ca39702`, this file's own rewrite). The five beyond that table:

| Commit | What |
|---|---|
| `e33de83` | Patreon welcome rebuilt as an embed ("bonded with this spider"), DM'd, and **re-sent on re-link** so someone who subscribed before linking still gets it |
| `f330cda` | Ally-decay drawback reframed — allies want visits because they're holding onto Peter Parker, not to unleash what's inside |
| `ad01949` | Spider Bots / Electric Webbing proc rates fixed (they were failing so often they read as broken), and Biomorphic Webbing made to subsume Organic |
| `eee4590` | Sonic Dampener now fires on Shocker **rematches**, not just the first encounter — ⛔ **the mechanic itself was deleted in round four**, so this commit is history only. It's worth reading before proposing anything similar: the bug it fixed meant the dampener had fired *exactly once in the entire game*, at reputation level 20, for every player |
| `d88cc29` | Camera Gold shipped — the deferred item from the last snapshot, unblocked by #1 |

**Repo state (2026-08-30)**: branch `main` at `d91a092`, in sync with `origin/main`. **27 files uncommitted, all round nine** — 24 modified code files (`bot.py`, `config.py`, `data/items.json`, `cogs/ally_cog.py`, `cogs/bugle_cog.py`, `cogs/daily_cog.py`, `cogs/help_cog.py`, `cogs/lab_cog.py`, `cogs/patreon_cog.py`, `cogs/patrol_cog.py`, `cogs/pvp_cog.py`, `cogs/tutoring_cog.py`, `db/models.py`, `services/ally_service.py`, `services/battle_service.py`, `services/brewing_service.py`, `services/bugle_service.py`, `services/daily_service.py`, `services/economy.py`, `services/patrol_service.py`, `services/shop_service.py`, `services/tutoring_service.py`, `utils/first_run.py`, `utils/icons.py`, `utils/item_display.py`) and 3 new (`services/server_perks.py`, `cogs/perks_cog.py`, `alembic/versions/5c2ab7f10de4_add_perk_pair_choice.py`) — plus both docs. Nothing deleted. The fan-out is wide because the perks are *threaded* rather than cached, so every service on a perk's path gained a parameter defaulting to `NO_PERKS`; it is not two dozen independent changes. `alembic heads` = **`5c2ab7f10de4`** (`add_perk_pair_choice`, round nine's second pass), 11 migration files. **Rounds two through eight needed no migration between them**, which is worth understanding rather than just recording: round two's revocation is derived from the live tier rather than stored anywhere; round three's accent is derived per-command from that same tier and stored nowhere at all; round four adds no persistent state either — the ambient scavenge writes through the existing `inventory_items` table, and the repair reskin and the glyph rule are pure copy. Round five is the same: the camera fix *deletes* an `inventory_items` row rather than adding a column to mark it, and Venom Blast's charge lives on the in-memory `BattleState` exactly where it already did. Round six adds no state and no write at all — its counter is a `SELECT COUNT(*)` over rows round one's migration already created. Round seven does add a write, and still needs no migration: its DM throttle is a row in the existing `cooldowns` table under a new `stealth_dm` key — schema the game has had since the beginning (§20.6). **Round eight is the strongest case of the pattern**: it sells a real-money product and stores *nothing* about it, because fulfilment is a hand-run `/admin inventory give-item` writing through `inventory_items` like any other grant. If an order ever needs to be *recorded* — refunds, disputes, "did this person already get theirs" — that's the migration this feature doesn't have. **Round nine is where the streak ends, and how it ended is the useful part.** Its first pass held the pattern deliberately against a real temptation: a two-column cache on `users` was designed and then rejected, because ally decay is integrated over the whole elapsed window and a stale flag would apply a perk to hours spent outside the server. Perks resolve per-command from the interaction payload and persist nowhere, the one thing the round *writes* — the Bronze camera — goes through `inventory_items` like every other grant, and it even **retires** a column rather than adding one (`PatreonLink.growth_perk_choice`, now read by nothing, still on disk). Then the owner asked for a switchable perk choice, and **a choice is by definition state the roles don't carry** — `users.perk_pair_choice`, nullable with no `server_default`, migration `5c2ab7f10de4`. *"No migration" was never the principle it read as here; it was a property of a design where every input already arrived in the payload.* **Both migrations are applied to the dev DB** — round one's after a backup at `scratch/spidey.db.bak-before-a7c41e93b508` (purely additive, 6 users and 179 transactions preserved, 14 tables), and round nine's after `scratch/spidey.db.bak-before-5c2ab7f10de4`, taken with sqlite's own `.backup()` rather than a file copy because the WAL sidecar holds committed data a `cp` would miss. Verified additive the same way: **9 users, 322 transactions, 14 tables** before and after, `perk_pair_choice` present on `users` and NULL for all 9. `alembic current` = `5c2ab7f10de4` = head.

## The follow-up batch (round two) — written, verified, **committed 2026-08-28**

Five reports from the owner playing the shipped tier. Each is listed with what was actually wrong, since three of the five turned out to be a different problem than the report described.

**1. The ally-decay footer showed the wrong tier's badge.** Symbiote inherits Arachnid's happiness-drain cost, and §9's attribution rule is *emoji-only* — the copy never names a tier — so the badge is the entire signal for whose subscription is talking. A Symbiote subscriber was being shown an Arachnid badge on their own drawback. Now `tier_badge(tier_rank)`: the viewer's own tier. **Note the command**: this footer is in `/ally check`, not `/ally visit` (which has no tier footer at all).

**2. Arachnid+ perks in the shop now show both badges.** Same table, opposite row: a *static catalog* answers "who is this available to?", so it lists **every** badge that clears the gate (`tier_requirement_badges(min_rank)`). Showing only Arachnid told a Symbiote subscriber they couldn't buy something they could. These two fixes are one rule with two halves, now written into §9 as a table — and both bugs came from the same wrong assumption, that the gate's tier and the player's tier are the same thing. For a superset tier they never are.

**3. The Spider Bots report was three separate things, and the break path wasn't one of them.** The report was "they don't get removed from the inventory." They did — `roll_gadget_wearout` deletes and commits correctly. What the live dev DB actually showed was **two** `spider_bots` rows for that user, both unequipped: spares from separate purchases. `/inventory` rendered them as two byte-identical `x1 (100 durability)` bullets, so losing one changed nothing visible. Three fixes:
   - Render-identical rows fold into one line with quantities summed, and `(Lv. N)` now appears so genuinely-different copies stay distinct.
   - **"Swinging-Grade" is gone** (with "Battle-Grade"), replaced by "Equipped"/"Stowed". The owner's read was exactly right: those were slot states dressed up as *item quality grades*, and no such grades exist. Spider Bots are combat gear whether or not they're in a slot.
   - One **real latent bug** found along the way: a `quantity > 1` gadget row was deleted **wholesale** by a single break. Now it decrements. Only reachable via `/admin` grants or seeded data (the shop never stacks gadgets), which is exactly why nobody would have connected the loss to a break.

**4. Every camera mention now shows the camera they own.** Patrol cards, `/bugle photos` and the photo-bump row all drew the base beat-up 35mm, and one line hardcoded the words *"the silver lens"* — so a Symbiote subscriber on a $3,000 Gold body saw someone else's equipment on every photo. The camera now resolves **once, server-side** (`BattleReport.camera_item_key`/`.camera_label`, set in `finalize_battle`, which is the only place with both the session and the live tier check) and travels with the result. `EffectiveCamera.label` reads `Item.name` from the database rather than hardcoding a string, so shop, inventory and battle copy can't drift apart.

**5. Perks are revoked at use time, for both lapsing and unlinking.** One live rank check covers both, and that's the whole design: a cancelled pledge clears the stored tier (`refresh_stale_links`) and `/patreon unlink` deletes the row, so `get_tier_rank` returns rank 0 either way — nothing needs to tell them apart. `patreon_service.locked_item_keys()` is the shared query and short-circuits without a DB round-trip when nothing in the list is gated, which is the normal case. Two enforcement points: `list_usable_gadgets` (gadgets) and `get_effective_camera` (cameras).

Three decisions inside #5 worth not re-litigating:

- **Inert, not deleted.** A locked gadget keeps its row, slot and upgrade level. Resubscribing switches it back on rather than asking someone to re-buy gear they paid cash for — but the load-bearing reason is the slot: hiding it from `list_equipped_gadgets` would free one, let them equip a third gadget, and hand them **three live gadgets** on resubscribe. Hence the split between "what's equipped" (honest, feeds `MAX_EQUIPPED_GADGETS`) and "what may fire".
- **A locked gadget can't break; a locked camera can.** Deliberate asymmetry. The gadget does nothing, so breaking it would make lapsing *destroy* gear instead of pausing it. The camera is still earning photos at base stats, so it carries base-camera risk — making it invulnerable would turn lapsing into its own perk.
- **Revocation is made visible, and cash spend on inert gear is blocked.** Scope taken on beyond the literal report, because the button-suppression makes a revoked gadget *more* invisible, not less — one that silently never fires is indistinguishable from a broken one. So `/gadget status`, `/gadget panel`, `/inventory`, autocompletes and Select descriptions all label it. `upgrade_gadget` rejects with its own message rather than reusing the equipped-list filter, which would say "that gadget isn't equipped" about a gadget visibly in a slot — and this is a **cash spend**, the worst version of the bug. Equip/Unequip stay live: freeing the slot is the one useful action.

`/inventory` is the only surface that explains a revoked **camera** — cameras have no status command of their own.

**Also moved**: `GATED_ITEM_MIN_RANK` migrated from `shop_service` to `patreon_service`. Use-time enforcement gave it a second consumer, and a map that both the shop and the battle loop read belongs with the tier logic.

## Round three — the tier accent, and the upgrade welcome

Two pieces of work, both in the working tree alongside round two.

### The Patreon accent on every Components V2 panel

Subscribers had functional perks but nothing *visual*. Now every V2 container a subscriber sees carries their tier's colour as its accent bar: Arachnid `#A91D3A`, Symbiote `#707C8F`, no subscription = no bar (unchanged from before).

Two structural facts turned what looked like a 57-call-site sweep into ~14 real edits, and both are worth knowing before touching V2 code again:

- **`Container(colour=None)` is byte-identical to `Container()`.** pycord's `__init__` does `accent_color=colour or color` (`.venv/.../discord/ui/container.py:117`), so "no subscription" needed no special-casing anywhere — the free-account render is provably the same as it was.
- **`utils/v2_embeds.py` already funnelled 38 of 41 containers**, so all 36 `StaticView(...)` call sites across 16 cogs needed **zero** edits.

The design decision worth not re-litigating: the accent is resolved **once per command** into a `contextvars.ContextVar` (`utils/tier_accent.py`), set by the single global `before_invoke` hook in `utils/first_run.py`, and read by `utils.v2_embeds.make_container()`. Threading an `accent=` argument through 36 call sites would have meant 36 chances to miss one, and **a missed site is invisible** — a dropped accent renders exactly like a free account. This is safe because pycord's invoke path runs `call_before_hooks(ctx)` and then the callback in the *same* task, and each interaction gets its own task with its own copied context, so one user's colour cannot leak onto another's panel.

**The contextvar is dead inside component callbacks.** Button presses and `on_timeout` run later, in fresh contexts. So every interactive view captures `self.accent` at construction and re-renders from that field — nine of them do (`PaginatedView`, `LeaderboardView`, `GadgetPanelView`, `ShopBrowseView`, `HelpBrowserView`, `StartView`, `AdminHelpView`, `UserInfoView`, `AdminsListView`). Two sites needed more than the mechanical change: `PatrolBattleView` derives from the `tier_rank` it already received rather than adding a redundant field, and `help_cog`'s `OpenGuideButton.callback` — the one view in the codebase built outside a command body — forwards the accent captured by the `StartView` the button lives on.

`accent_for_rank()` in `patreon_service` is an **exact-rank dict lookup, and must not become `>=`**. Every perk *gate* in that file uses `>=` because a gate asks "does this rank clear the bar", which Symbiote must satisfy for Arachnid gates. The accent asks "which tier *is* this player", which has one answer — a `>=` walk would paint Symbiote subscribers Arachnid red. There's a comment saying so, because someone will otherwise "consistency-fix" it.

Two accepted consequences:

- **Public messages carry the invoker's accent.** Most V2 responses are non-ephemeral, and a single Discord message cannot be coloured per-viewer. So `/admin userinfo @target` shows the **admin's** colour, not the target's. Confusing *viewer* with *subject* is exactly what caused round two's bugs #1 and #2, so it's worth stating rather than discovering.
- **One extra query per command, for everyone including non-subscribers.** `get_tier_rank` is a single `session.get(PatreonLink, discord_id)` — a primary-key lookup, on a session `first_run` was opening anyway. Deliberately uncached: a cache would delay perk *revocation*, which is the thing round two just fixed.

**`/v2demo` is deleted** rather than made tier-aware (`cogs/v2_demo_cog.py` removed, `bot.py`'s `EXTENSIONS` 23 → 22; it is back to **23** as of 2026-08-28, since `ads_cog`/`invite_cog` arrived after this was written — the count in this file has been wrong once already, so count the list rather than quoting it). It was a temporary scope demo. It held 638 uncommitted lines, so a copy is parked at `scratch/v2_demo_cog.py.removed` (gitignored) — delete that once you're sure. The memory note that pointed future sessions at it as "the working reference" has been rewritten, since a pointer to a deleted file is worse than no pointer.

**Miss-guard for anyone adding a panel**: `grep -rn "discord\.ui\.Container(" .` must match **only** `utils/v2_embeds.py`. Any other hit is a panel that silently renders without a subscriber's colour. It's clean right now.

### The link-then-subscribe welcome

Someone who links Patreon *before* pledging gets the "Patreon Connected, nothing active yet" card — correct at the time. When the pledge lands, there's no interaction for the bot to respond to, and `handle_callback` has already stamped `last_checked_at`, so the background loop wouldn't look again for six hours and when it did it **only logged**. Perks switched on in silence; the sole recovery was re-running `/patreon link`, which nobody does after it already worked once.

Both halves are now closed:

- **A second staleness window.** A link with `tier IS NULL` has nothing to revoke — the only change it can produce is a *grant*. Making that wait six hours gets the asymmetry backwards: being slow to revoke protects someone who is paying, being slow to grant just strands them. Tier-less links go stale after `REFRESH_PENDING_STALE_AFTER` = **20 minutes**, bounded by `REFRESH_PENDING_WINDOW` = **48 hours** since linking. The bound is load-bearing: `refresh_link` *also* nulls the tier on a dead grant (`_DeadLinkError`), and a row sitting tier-less for days is far likelier to be one of those — or someone who linked and never pledged — than a pledge about to land. No dead-link flag is persisted, so link age is the only available discriminator.
- **`patreon_tier_upgraded`.** `patreon_tick` dispatches it whenever `outcome.rank_delta > 0`, and `PatreonCog.on_patreon_tier_upgraded` DMs the welcome for the user's current tier. Routed through the **event bus** rather than reaching into the cog for `_send_welcome`, for two reasons: the welcome copy stays owned by the cog that owns everything else about it, and because dispatch schedules the listener as its own task, a closed-DM `HTTPException` cannot take down a `tasks.loop` that dies permanently on one uncaught exception. No channel fallback, unlike `/patreon link`'s copy — nothing the user did triggered this, so a bounced DM just logs.

The 20-minute window sits deliberately *above* the 15-minute tick: a pending link comes up on roughly every other tick, so **20–35 minutes** from pledging to the DM. Dropping it under the tick interval wouldn't be faster, only more expensive — the queue can't drain more often than it runs. `/patreon link` remains the instant manual path, and is now commented as the fallback rather than the only route.

**A regression caught by the checks, worth remembering.** The first version of the query made the six-hour rule conditional on `tier IS NOT NULL`, with the fast lane as the other branch. That dropped tier-less links older than 48h out of the queue **entirely** — strictly worse than before the change, since the original query re-checked every row unconditionally. The fast lane has to be an *added* `OR` branch, never a replacement. The fixture row that caught it ("pending, linked 10d ago, checked 7h ago") exists precisely because the comment claimed a fallback the code didn't implement.

## Round four — the seven Symbiote fixes

Seven more reports, all against the Symbiote tier, with an explicit instruction to take them one at a time. Four needed a decision from the owner and every one of them picked the more aggressive option: **delete** the Sonic Dampener rather than just its copy, **raise the override rate *and* extend it to gadgets**, **lower** Venom Blast's trigger bar, and **no admin tier command** (a real pledge is linked, so live testing is possible).

Three of the seven were reported as unimplemented and were already working. That's the finding worth carrying forward, not the individual fixes:

| # | Reported | Actually | Fix |
|---|---|---|---|
| 1 | Gold Camera doesn't retire Silver | True only via `/admin inventory give-item` — buying it always worked | `install_tool` extracted; the admin path routes through it — ⛔ **round five had to reopen this**: "retire" meant unequip-and-keep, and the kept row still showed in `/inventory` |
| 2 | Welcome note still cites Sonic Dampeners | Copy was stale | Mechanic deleted; copy replaced by the override |
| 3 | The suit never overrides anything | **Implemented and firing** at 9.94% of Evade presses | Rate 0.10 → 0.15, extended to gadget presses — ⛔ **round five doubled it again to 0.30** |
| 4 | Biomorphic Webbing is just organic webs | True gap — every roll was combat-only | New `services/biomorphic_service.py`, wired into 3 activities |
| 5 | Venom Blast not implemented | **Implemented**, 3.4% of bracket-1 fights | Trigger 0 → 25% integrity — ⛔ **round five made it a button**, so it no longer fires on its own at all |
| 6 | Repair branding wrong for Symbiote | `suit_service` had zero tier awareness | Full copy reskin, mechanics untouched |
| 7 | Symbiote emoji unused | All four assets existed, zero call sites | Two-glyph attribution rule, six render sites |

**The three combat changes were tuned as one package, not individually**, because they all move the same number: deleting the dampener is a buff, lowering Venom Blast's bar is *not necessarily* one, and the override changes are nerfs.

**Venom Blast's threshold is the most counter-intuitive number in this batch, and the sweep is why.** ⚠️ *Every win-rate figure in this paragraph was measured against the **automatic** mechanic round five replaced with a button; the reasoning that picked 25% survived that change and is why the button's arming bar is still 25%, but the numbers did not — see round five below.* The charge is once per fight, so a bar set high spends it on a survivable hit and leaves a lethal one later unabsorbed — bracket-10 win rate was genuinely **U-shaped** across `{0,15,25,35,50,65,80}`, every step more than 3σ. The optimum was 65% (a free +0.14% over shipping 0%) and it was **rejected anyway**: at that bar 78.6% of blasts fired on round 1–2, which turns a life-saving intervention into a routine opening move and makes "swallows the hit whole" dishonest. **25% shipped** — median round 4, only 10.6% firing on rounds 1–2, the last threshold that stays a mid-fight event. It was also the *worst* point on the win-rate curve (−9.06% at bracket 10), and that was a deliberate progressive trade: the cost landed only on high brackets where the blast already fired in 99.1% of fights, while low brackets gained both visibility (3.3% → 22.6%) and win rate. Symbiote at bracket 10 won 60.86% against 3.78% unsubscribed.

**The override's gadget hijack answers the objection that kept gadgets exempt, rather than overruling it.** The recorded reason was that gadgets are scarce and carry durability, so swallowing one "reads as a lost *item* rather than a lost impulse". So the branch sits at the very **top** of `resolve_gadget`, above `roll_gadget_effect` and `roll_gadget_wearout`: a hijacked press costs the round but **never the gadget**. `check_override.py` proves the placement by replacing both rolls with spies and asserting a hijacked press calls neither — a source read can't prove it, since an early return could still sit below a call.

Extending it to gadgets is also **what makes the override a real cost outside boss fights**, which it wasn't before: in a crime patrol suit integrity is cosmetic, so losing an Evade's damage reduction costs almost nothing, but losing a gadget's damage costs the same everywhere. At an unchanged 0.10 the crime cost went from **−0.51% to −2.36%** purely from the new surface. 0.15 measured at −6.30% on a boss (51% of fights see at least one hijack); **0.20 was measured at −8.76% and rejected** against the −7.87% bar. That bar had previously rejected 0.15 too — it only clears now because it was re-measured against the post-dampener configuration. ⛔ **Round five overturned all of this**: the owner asked for 0.30 directly, which breaks the −7.87% bar by a wide margin and was shipped anyway as an instructed decision. The bar is now a record of what was measured, not a gate anything passes.

**Biomorphic Webbing's ambient scavenge had to be a new leaf module.** `patrol_service` imports `ally_service`, so `ally_service` can never import back — `services/biomorphic_service.py` imports only `random`, `inventory_service.add_item` and one constant. A 20% roll on `/tutoring`, `/ally visit` and `/bugle submit` for one repair component (**raised to 30% in round six**), weighted 3:1 toward Spandex Fabric over the pricier Micro-Electronics, with activity-specific flavor. **Every call sits after the activity's own commit**, so a bonus can never roll back a completed activity.

**The repair reskin is copy-only and that's enforced by test, not by reading.** `/workbench` becomes "The Bond" for Symbiote subscribers — Peter doesn't understand the thing, but it still demands spandex and micro-electronics. `check_symbiote_repair.py` runs the identical scenario at all three ranks and asserts **one distinct outcome tuple** across them, so a copy change can't quietly become a mechanical one. **"Suit Integrity" was deliberately left alone** (a documented deviation from the plan, which listed it): it's the shared stat name and renders in `/balance`, the boss gate and `/admin profile`, so reskinning it in one panel of four would make one number look like two stats.

**Item 7 turned out to be an attribution rule, not an icon sweep.** Before it, the tier badge did two jobs — "a perk fired" and "whose subscription" — so a Venom Blast, a bonus component and a bonus photo all carried byte-identical attribution. Now the **perk's glyph leads** the line and the **tier badge trails** it, and the tier's name is never spelled out. Perks with no glyph of their own (the override, the repair reskin) correctly render the badge alone.

The one deliberate inversion is `/shakedown`: **Stealth Mode belongs to the victim, not the reader.** `ShakedownResult` now carries `target_tier_rank` because the cog physically cannot look it up off `ctx.author`. It's returned on the result rather than re-read in the cog because `get_tier_rank` consults the Patreon cache, which the background loop can move between the gate and the render — the badge has to name the rank the gate actually judged. Populated on **both** branches, since a field set on only one is a trap for the next caller.

## Round five — the camera, the button, and 0.30

Three requests, again with the instruction to take them one at a time. All three land on ground round four had just covered, and two of them **reverse** what round four decided.

**1. "I bought the Gold camera and I still see the Silver one in my inventory."** Round four's fix was real and its check script passed; the design was wrong. "Retire" meant *unequip and keep the row*, intended as a lapsed-pledge fallback. It never worked as one, and four separate facts each independently make the kept row unreachable: `get_equipped_camera` filters on `equipped`, **there is no `/equip` command** to turn it back on, `get_effective_camera`'s lapse path degrades the *equipped* body's stats rather than falling back to the retired row, and `economy_cog` only annotates a tool line when it's equipped or tier-locked — so a dead $1,000 Silver rendered next to the Gold looking exactly like working gear.

Offered labelling, hiding, refunding or deleting, and shown that deletion writes off $1,000, the owner chose **delete the row**. Three consequences worth carrying:

- **Deletion makes a downgrade purchase destructive**, so `buy_item` refuses one outright rather than allowing-and-guarding. A downgrade was already a pure loss before deletion (no equip command to switch back), it just wasn't a *destructive* one, so blocking it takes nothing of value away.
- **`CAMERA_FAMILY_KEYS` is ordered cheapest-first, and the list index IS the tier.** That's now load-bearing in three places — `get_equipped_camera`'s tiebreak, `install_tool`'s lower-tiers-only delete slice, and `buy_item`'s downgrade refusal. Reordering that list would silently make a $1,000 Silver purchase scrap a $3,000 Gold body.
- **A silent destructive write-off is the next bug report**, so it isn't silent: `install_tool` returns the keys it destroyed and both call sites render `scrap_note()` — *"Stripped Silver-Grade Camera for parts."* The shop descriptions in `data/items.json` were saying the old body was "retired", which was the same misleading word the code had used; both paid cameras now say the old one is gone, not stashed. (`db/seed.py`'s `seed_items()` upserts `description` on every startup, so that propagates to existing DBs with no migration.)

**The admin path had the same bug in a second form, and only running the code found it.** `/admin inventory give-item` names its local `scrapped_note` — because the first draft named it `scrap_note`, shadowing the imported function, so the first tool an admin granted would raise `TypeError: 'str' object is not callable`. `compileall` cannot see a runtime shadowing error and neither can a source read that isn't looking for it; the check script caught it in `shop_service` and the same mistake in `admin_cog` survived until it was read line by line. **This path has no automated coverage of its own** — `check_camera_gold` drives `install_tool` and `scrap_note` directly, not the cog.

**2. Venom Blast is a button now, and that makes it a much weaker perk.** It sits beside Evade in boss fights only, present from **round 1** in a greyed-out `Venom Blast — Not Charged` state, arming at ≤25% integrity and reading `Venom Blast — Spent` after use. Three states, all greyed but the middle one; `style=secondary` because it's the only style left (Attack owns `danger`, Evade `primary`, gadgets `success`) and grey happens to be right anyway — inert until it isn't. A button that only *appears* once armed couldn't teach anyone the condition exists, so it's always there and a subtext line under the row says what charges it.

The cost of the change is large and it is a direct consequence of the requested design, not a bug. At the same 25% bar, bracket-10 win rate goes **60.86% → 13.91%** against a 3.93% unsubscribed floor. The automatic version returned `damage=0` on the hit that would have ended the fight — it was a **once-per-fight death save** wearing a damage perk's copy. A button spends the same charge on *offense* while the lethal hit still lands. Getting the power back would mean giving the press a defensive component; nothing else moves that number.

Two things about the numbers:

- **An armed press is exempt from the suit override.** Asserted, not assumed — the check forces the override rate to 1.0 and fires 20,000 armed presses. A hijacked Venom Blast would consume the once-per-fight charge for nothing, which is the one override outcome that costs a *resource* rather than a round.
- **Every button-era figure is a floor, not an estimate.** `run_fight` presses the blast the round it arms — the greediest possible policy. A player who holds it reaches strictly better outcomes, so the sim under-reports the perk.

**3. The override rate is 0.30**, double round four's 0.15, as instructed. This knowingly breaks the file's own **−7.87%** bar and fails the round-four plan's first acceptance criterion (a paid tier must never be net negative). The measured cost, by press type — and **compare rates within a column, never across**, since delta magnitude scales with the baseline's headroom:

| rate | evade-only, boss | with gadgets, boss | evade-only, crime | with gadgets, crime |
|---|---|---|---|---|
| 0.10 | −2.16% | −10.59% | −0.51% | −2.36% |
| 0.15 | −3.28% | −15.91% | −0.80% | −3.73% |
| 0.20 | −4.64% | −20.16% | −0.89% | −4.87% |
| **0.30** | **−6.82%** | **−29.02%** | **−1.74%** | **−6.88%** |

What 0.30 buys is visibility: **1.20 hijacks per fight and 78.8% of fights showing at least one**, against 0.43/62.8% at 0.10. What it costs is concentrated almost entirely in the gadget hijack. Gadget-free, the Symbiote tier is net **−0.38% / +26.76% / +10.30% / +8.81%** at boss brackets 1/5/10/20; with three gadget presses a fight it's **−1.05% / −11.86% / −6.41% / −3.35%**. Crime patrols stay strongly positive either way (**+3.83% / +20.26% / +42.81%** across the three tiers). So the drawback lands hardest on the player who bought the most gear, which is backwards. **If this needs softening later, narrow the hijack back toward Evade-only rather than lowering the rate** — the rate is what makes the perk visible, and the gadget surface is what makes it expensive.

## Round six — Stealth Mode gets a face and a scoreboard, and the scavenge rate lands

Two asks. The first: *"if the stealth mode triggers, the embed should show the stealth mode emoji, symbiote branding etc etc. does that happen?"* The honest answer was **half yes**. Round four had already wired the glyph and the badge and pinned both by test, so the attribution rule was satisfied. Two things it hadn't touched turned out to matter more than the emoji. The second ask raised Biomorphic Webbing's ambient-scavenge rate, and is item 3 below.

**1. The panel was a refusal wearing a perk's glyph.** The branch called `error_embed`, which titles **all five** of `/shakedown`'s refusals *"Parker Luck."* — so the only Symbiote perk a thief will ever watch fire was framed as the thief's own bad luck. A legacy embed also can't carry the tier accent bar or a thumbnail, which are the two things that make a panel *look* paid for. It's now a `StaticView` titled **"Stealth Mode"** with the `stealth_mode` thumbnail. The other two branches were deliberately left alone: a successful shakedown was already a `StaticView`, and a *failed* one is a real refusal that `error_embed` fits.

**The trap inside that fix is the accent, and it is the kind that never looks broken.** `make_container()`'s default accent comes from an ambient `ContextVar` set once per command from the **invoking** user (round three's design, and the right default everywhere else). On `/shakedown` the invoker is the *thief*. Left to default, a subscribed thief would see **their own** tier colour on a message about somebody else's subscription, and an unsubscribed thief would see no bar at all on a panel whose entire subject is a subscription — and in both cases the panel renders cleanly, so nothing would ever be reported. So the branch passes `accent=accent_for_rank(result.target_tier_rank)` explicitly, and the check asserts both halves: that the accent is read off the target **and** that it is not read off the invoker. All three marks — glyph, badge, accent — now name the one person who paid.

**2. The perk had no surface for the person paying for it, and that's the part worth remembering.** A protected attempt answers the thief and tells the target *nothing* — no DM, no ping, not even a `transactions` row — and the gate only opens once the target has been idle 20+ minutes. A subscriber could hold the tier for a month, have a dozen shakedowns turned away, and have no way to learn any of it happened. Every other perk in the tier announces itself the moment it fires.

`/patreon perks` now reads it back: `count_stealth_protections()` counts the subscriber's `OUTCOME_PROTECTED` rows and the line becomes *"It's turned away 3 attempts so far — you weren't there to see any of them."* **No new table and no new write** — round one's threshold instrumentation (`shakedown_attempts`, migration `a7c41e93b508`) turned out to already be exactly the data its owner had a stake in; it was only ever being read by an analysis script. The welcome DM carries it too, since `/patreon link` re-sends that card on demand.

Four small decisions inside it:

- **Zero renders no clause at all**, not "blocked 0 attempts". A brand-new subscriber reading the welcome DM shouldn't be handed a nil stat about a perk they haven't had time to benefit from.
- **The 20 is interpolated from the constant, not typed into the copy.** Hardcoding it is exactly what went stale on the Venom Blast line when its multiplier moved, and on the ally-decay drawback before that. The check proves the copy tracks the constant by moving it to 45 and re-rendering.
- **The read is as non-load-bearing as the write.** `count_stealth_protections` opens its own session and swallows everything, for both of `log_shakedown_attempt`'s reasons: the table is allowed to be missing, and a failed query on a *borrowed* session poisons the caller's transaction. A perk count must never be able to take down the panel listing the perks. It returns 0, which renders as the old line rather than as a wrong number — and that path is tested by breaking the query outright, not just intended.
- **The tier gate lives in `_stealth_protections()`, not at the two call sites**, so neither can forget it and spend a query on a card that has no Stealth Mode line to put the answer on. `SYMBIOTE_PERKS` split into `SYMBIOTE_PERKS_STATIC` + `_stealth_mode_line()`; the new `stealth_protections` argument defaults to **0** all the way down, so every existing positional call still works and renders identically.

**3. And the rate the owner asked about landed at 0.30.** Same round, unrelated perk: *"we need to up the percentage a lil bit"* on Biomorphic Webbing's ambient scavenge, answered **0.30**. `AMBIENT_SCAVENGE_CHANCE` moved **0.20 → 0.30**, which puts the two components at **22.5% / 7.5%** per activity (the 3:1 weighting is the split, not the rate, so those per-item numbers moved on their own).

**The player-facing copy needed no edit, and that is the whole reason it was written the way it was.** `BIOMORPHIC_WEBBING_LINE` quotes no percentages at all — a deliberate choice made in round one after Venom Blast's hardcoded threshold went stale the moment its multiplier moved, and the ally-decay drawback before that. So a rate change touched the constant, three documentation comments, §9.3, and `check_ambient_scavenge.py`'s band, and nothing a subscriber reads. The check now asserts the rate as an **exact** value with the ±2.2pp band derived from it, so the next move has to come to that script and choose a new band rather than sliding under a tolerance that keeps passing. Measured after the change: **29.7% / 30.5% / 29.0%** across the three activities.

**0.30 makes this the perk's highest rate**, above the cash roll's 0.25 and the two combat rolls' 0.20, where it used to be tied for lowest. The old argument for 0.20 — one recognisable rate across all four rolls — was already false when it was written, since the cash roll has always been 0.25. What makes the new ordering defensible rather than merely instructed: **the two 0.20 rolls carry extra preconditions on top of the rate** (the component only rolls if the base drop already missed; the photo needs a camera equipped *and* a photo already banked) while this one fires on every completed activity with nothing else asked of it. A flat rate across rolls with unequal preconditions was never the same odds anyway.

**One correction to the numbers this decision was presented with.** The EV figures quoted while the choice was open — $19.50 → $29.25 per activity — are **catalog valuations, not income**. There is no NPC buyback anywhere in the game; `market_service` is player-to-player only, so the $80/$150 in `data/items.json` are shop *buy* prices and a surplus component cannot be turned into cash by the bot. What actually bounds the rate is the sink: **a repair consumes exactly one Spandex Fabric** (plus one Micro-Electronics past `suit_service.ELECTRONICS_THRESHOLD`) **regardless of how much integrity is missing**. So 0.30 buys a Symbiote subscriber repair self-sufficiency and adds supply to the player market. The fastest farm is giftless `/ally visit` — free, no cooldown, busy lock bottoming out at `MIN_VISIT_SECONDS` (30s) once the ally is happy, so ~120 rolls/hour by clicking, now ~36 components/hour instead of ~24. That loop predates the rate change and is bounded by the same one-item sink.

## Round seven — the DM Stealth Mode owed its owner

*"add the dm message whenever someone tries and shake down a symbiote user"*

Round six gave the perk a scoreboard on `/patreon perks`. This is the other half, and the ask is the right one: **a pull surface only helps someone who thinks to go and look, and nobody goes looking for a perk they don't know fired.** When Stealth Mode turns an attempt away, the target now gets a DM — which perk stopped it, who came looking, that nothing was taken, the 20-minute rule, and the same all-time count the card shows.

**Scope, stated plainly because it's narrower than the sentence might suggest.** The DM fires only when Stealth Mode **blocks** an attempt. A shakedown that *succeeds* against a Symbiote subscriber still tells them nothing, and that is a deliberate line rather than an oversight: being robbed while away is a gap that affects every player equally, so filling it is a game-wide feature (or a new perk), not part of making this one visible. Say so if the owner meant the broader thing — it's a small change to make, but it isn't this one.

**Naming the thief was the one call worth checking, and it leaks nothing.** `/shakedown`'s response is **not ephemeral** — the channel already saw a panel titled "Stealth Mode" that names the target and tells the thief *"Something's watching"* them and that they back off *"before you even get close"* — posted while the target was away. The DM reports a public message posted where they weren't looking, and who came sniffing is the only part with any value to them.

**§9's attribution rule runs in its normal orientation here**, unlike the thief's panel, which stays the codebase's one inversion: the reader *is* the payer, so the glyph leads and their own badge trails. **But the accent is the same trap round six hit, for the same reason and one step further out.** `_stealth_dm_view` is built inside the *thief's* command context, so `make_container()`'s ambient default would paint a subscriber's own perk DM in the thief's colour — or in no colour at all if the thief doesn't subscribe. It takes the accent explicitly.

Two details from that, both worth carrying forward. The parameter had to be named `target_tier_rank`, not `tier_rank`, because `check_symbiote_icons.py` asserts the literal string `accent_for_rank(tier_rank)` appears **nowhere** in `pvp_cog.py` — round six's own guard against reading the invoker's rank, which a plausibly-named new parameter would have tripped. **That's a check earning its keep in a way it wasn't written for**, and it was caught by reading the assertion rather than by running it. And the ambient default being *silently* wrong is now twice in a row on this one command; treat any new render inside `/shakedown` as accent-suspect by default.

**The throttle is the part that needed real thought.** The perk only fires while its owner is idle 20+ minutes, so a run of attempts during one absence would arrive as a **stack of near-identical DMs waiting for them** rather than as a live feed. One DM per 15 minutes per target, on a `cooldowns` row keyed `stealth_dm`. The real floor is tighter than that constant alone: `TARGET_PROTECTION_SECONDS` already caps any target at one attempt per 2 minutes from anyone, so 15 minutes turns a worst case of ~30 DMs/hour into at most 4, while an ordinary once-in-a-while attempt still always gets its own.

**Throttling is only acceptable because it's lossless in aggregate** — the DM quotes the **all-time** total, not "this is number N", so a subscriber attempted five times and DMed once still learns the true figure, and it's the same figure `/patreon perks` gives them.

Four smaller decisions:

- **The slot is claimed in the service, not the cog.** `claim_stealth_dm_slot` is named for its side effect: check-then-write has to be one step or two thieves resolving back-to-back both pass it. It sets `ShakedownResult.notify_target`, so the cog does only the Discord half and cannot forget the throttle.
- **Only the protected branch may consume the window** — and this is the one that would have quietly reintroduced the bug being fixed. If an ordinary shakedown burned it, the target would miss the next attempt the perk actually turned away, which is the only thing this DM exists to report. The check asserts an active Symbiote target and a non-subscriber leave **no `stealth_dm` row at all**, not merely an unexpired one.
- **It claims *after* the log write**, because the count in the copy has to include the attempt being reported.
- **Unlike round one's and round six's Stealth Mode helpers, this one takes the caller's session and is allowed to raise.** Those are telemetry, where losing a row beats failing a command. This is a throttle on player-visible behaviour, writing to a `cooldowns` table `/shakedown` already touches twice in the same transaction — a swallowed failure here would leave the throttle unset and DM on every attempt from then on, which is worse than the command failing.

**The send runs after `ctx.respond`, always.** Answering an interaction has a 3-second deadline and this needs a `fetch_user` round-trip plus a DM send; doing it first risks failing the thief's command in order to notify the target. Closed DMs are the *expected* failure (logged at info, not exception), and a bounced send still burns the 15-minute window — correct rather than merely tolerable, since closed DMs are a persistent state and releasing the slot would just mean retrying a doomed send forever.

**One test in this batch was written wrong first and is worth the mention.** The "a failed DM can't break the shakedown" checks were originally `check(label, True, True)` — which passes just as happily if the helper returns early and never touches Discord at all. They now assert against a stub that **records** that `send` was reached, and with which kwargs (`view` *and* `files`, or the thumbnail's `attachment://` URI has nothing to resolve against). A test whose failure mode is "passes for a reason unrelated to the claim" is the same family as round five's green `check_camera_gold` — that one asserted the wrong design, this one asserted nothing at all, and both agree with you either way.

`check_stealth_instrumentation` went **62 → 105 checks**. One of them is subtler than it reads: the 15-minute window is expired by **aging the row into the past**, not by lowering `STEALTH_DM_COOLDOWN_SECONDS` — the constant is only read when a slot is *claimed*, so lowering it wouldn't expire an already-open window and the test would have passed for the wrong reason.

## Round eight — the game starts taking real money (2026-08-28, committed `d91a092`)

*"im selling web fluid vials for money on patreon… since we cannot implement an automatic flow, i will give the users the money manually"* — and, separately, *"add a system in /admin inventory give-item where i can use any user's discord user ID to give items"*.

Two asks, one feature. The first is a storefront; the second is the only thing that makes the storefront deliverable. Full spec in [GAME_DESIGN.md](GAME_DESIGN.md) §11.1, §19.3, §19.7.

**The one fact to carry forward: fulfilment is manual and nothing in this repo automates it.** No webhook, no entitlement, no order record. The bot cannot tell whether anybody has bought anything, and no code path reads `VIAL_BUNDLES` for permission — it is **copy, not entitlement**. Native Discord monetization (premium buttons + SKUs, which pycord 2.8.1 does fully support) was researched and set aside; this flow is off-platform on purpose. Anything added later that treats `VIAL_BUNDLES` as a source of truth about a purchase is wrong.

**The manual path was verified before the storefront that depends on it was built.** `web_fluid_vial` is a `collectible`, not a `tool`, so `/admin inventory give-item` routes to `add_item` (which stacks) and `quantity` has `min_value=1` with no upper bound — so a 300-vial order lands in one grant. That ordering mattered: if the vial were ever reclassified as a tool, give-item would route through `install_tool`, force `quantity=1`, and **a $19.99 order would deliver one vial**. `check_vial_shop` asserts the classification for exactly that reason, which is why a check about a *shop* asserts a fact about an *item type*.

**`Option(str)` for the raw ID, never `Option(int)`, and this is the sharpest technical point in the batch.** Slash-command option values arrive as JSON; JSON numbers are IEEE-754 doubles, exact only to 2^53. A 19-digit snowflake exceeds that, so an int option can silently hand you a *different* ID than the admin typed. Nothing about the failure looks like a failure — the command succeeds, the receipt is confident, and the vials are in a stranger's inventory. The same hazard applies to every future admin option that takes an ID.

**`fetch_user()` validation is mandatory, not politeness.** A typo'd-but-well-formed ID would otherwise mint a real profile for a nonexistent user, drop 300 vials into it, and print a receipt. Non-digits and anything outside 15–20 digits are rejected *before* spending the API call; a pasted `<@123…>` mention is accepted.

**My own check caught a bug I had just written, and the near-miss is the part worth keeping.** `if member is not None and raw_id:` treated a whitespace-only `user_id:"   "` as "both given" and refused an ordinary member-picker grant. Fix: normalize `provided = (raw_id or "").strip()` once, and use it for both the ambiguity guard and the fallback. But `cleaned = provided.strip("<@!>")` stays a **separate variable** on purpose — folding them together would mean a string of pure mention punctuation (`"<@!>"`) empties to `""`, falls through to `member or ctx.author`, and **silently grants the goods to the admin running the command**. `"<@!>"` and `"<@>"` are in the junk list now.

**`StaticView` grew a `link_button` argument rather than `/lab` growing a bespoke `DesignerView`.** Link buttons are the one kind it will ever take: no callback means no state and nothing for a timeout to protect, so the view stays as inert as it was. `link_button=None` is *provably* identical to the old behaviour — a structural-equivalence check mirroring the documented `Container(colour=None)` precedent — which is what made it safe to touch a class 22 cogs already use. Side effect: `patreon_cog.SubscribeView` is now replaceable by one `StaticView` call, and its docstring's claim that it *had* to be bespoke was false the moment this landed. Docstring corrected; the class left alone, because it works and `check_patreon_subscribe.py` drives it.

**The copy-interpolation rule (§9.3) binds harder here than anywhere it's been applied.** Every figure in all five pitches comes from a constant — bundle size and price from `VIAL_BUNDLES[0]`, per-batch yield from `brewing_service.YIELD_RANGE`. The comparison the pitch is built on ("a batch gets you 2-4, this gets you 30") is only tempting while it's true, and **a stale number in a price quote is a stale number the bot is asserting about real money.** `check_vial_shop` proves it the way §9.3 demands: it *moves* both constants, `importlib.reload`s the cog, asserts the copy followed and that the old price is gone, and restores in a `finally`.

Two things deliberately not built, both owner's calls:

- **No four-bundle price table in-bot.** Only the cheapest bundle is quoted, as an entry price. The shop page is the catalog; a price table inside a chem-lab card reads as an ad break rather than a nudge.
- **No price on the button, and no bare price anywhere.** `SHOP_BUTTON_LABEL` is `"Buy Web-Fluid Stash"` — the check asserts it carries **no digits at all**, an exact negative, so re-adding the price for "clarity" has to fail something first. And every pitch line must say the stashes *start* at $3 and go up, never that they cost $3: only one of four bundles is $3.00, so a flat quote is **false** the moment somebody clicks through to the $19.99 option. This is a factual constraint dressed as a tone note.

**The nudge went onto the refusals too, which reverses a call I had argued against in writing.** *"add it to /lab brew and /lab collect too"* — meaning the failure branches, the surfaces the first pass deliberately left bare. The argument against is still the right argument and it's still in the cog's comment block: turning a refusal into a sales pitch is the mistake round six fixed in the other direction on `/shakedown`, and "you can't afford this, here's how to pay us" is that mistake with money attached. What makes it defensible is that a refusal is the *highest-intent* moment in the command — the player wanted vials right now and the game said no — so the pitch answers the refusal rather than interrupting it. It ships quiet: plain prose, no bold, its own paragraph under the error, button alongside instead of the card restyled to sell. **If it ever reads as the bot needling somebody it just turned away, the volume of those two lines is the first thing to reconsider.**

**The refusals stay classic `error_embed`s, and the reasons are encoded as assertions rather than comments.** A V2 conversion would break the "Parker Luck." grey that every cog in the project shares as its error identity, and `make_container()` would paint a *refusal* in the caller's own Patreon tier accent. So `check_vial_shop` asserts the title and the exact colour on both refusal paths — a future tidy-up that unifies everything onto `StaticView` fails a check instead of just looking a bit off. `utils.embeds.link_button_view()` is the new counterpart to `StaticView(link_button=...)` for embed surfaces that should stay embeds; `patreon_cog.LinkButtonView` was left alone (it hardcodes its label), same restraint as `SubscribeView`.

**One assertion in `check_vial_shop` was wrong on its own terms, across every price check in the file.** It failed with *"brew-fail pitch dropped the old price"* — and the copy had not dropped it. `"$3" in "no $30"` is `True`: the substring test matched the old price *inside* `BREW_COST`. That's the round-five lesson again (a check can assert the wrong thing and be believed) but sharper, because here the check was *failing*, which reads as trustworthy. Fixed at both levels: a `quotes_price()` helper with a `(?![\d.])` lookahead now backs six assertions, **and** the copy stopped quoting `BREW_COST`, since two prices one clause apart with the smaller a substring of the larger is genuinely confusing to read regardless of what the checker thinks.

**Not tier-gated, and that's a design statement rather than a shortcut.** A one-time purchase and a recurring pledge are different products, so unlike every §9 perk surface, a Symbiote subscriber seeing this is being shown something they don't already have. Nothing here is made redundant by a subscription.

Round eight also fixed two check scripts that had started failing for a reason unrelated to any of this — see the pool/`StaticPool` note at the end of the verification section.

## Round nine — the community-server perk track, unblocked by a false blocker (2026-08-30)

*"implement the server perks… we will need 3 roles for this, server booster, level 10 and level 5. make it such that the .env takes the role IDs"*, then *"these perks must be available only to those who are running commands in the server with a guild id"*, then the one that mattered most: *"there is a way to implement it without intents. read the members role and guild when they run the command."* Full spec in [GAME_DESIGN.md](GAME_DESIGN.md) §22, with the design history in §9.5.

Seven free perks, gated on three `.env` role IDs in one guild. It is §9's sibling — same shape, different currency — and it is the first batch in this file whose *headline finding is that a documented blocker was wrong*.

**The blocker was false, and disproving it is the whole reason this shipped.** §9.5 had recorded since 2026-08-21 that rebuilding this track "requires re-enabling the Discord **Members intent**", which made it a live-bot config change rather than code and is why it sat unbuilt for nine days. The owner said otherwise and the owner was right. Discord ships the invoker's role list and guild-boost timestamp **inside every interaction payload**, and pycord stores both verbatim in `Member.__init__` — `discord/member.py:349` and `:346`. No gateway event, no cache warm-up, no privileged intent; the bot still runs `Intents.default()`. *A blocker recorded in a design doc is a claim someone made once, not a fact the code enforces. This one cost nine days and took two lines of library source to disprove — check the claim before you plan around it.*

**The scoping requirement then fell out of the transport instead of needing enforcement.** Because perks come from the payload of the command just run, they are a property of *that command* rather than of the account: run it in a DM or another server and there's no role list, so there are no perks. That is exactly what the owner asked for, and no guard implements it.

**`Member.roles` is the wrong primitive and picking it would have shipped a silent failure.** The public property resolves every id through `guild.get_role()` and *drops* whatever the cache doesn't have (`member.py:632-648`), so on a cold cache it returns a short list, raises nothing, and the perk simply doesn't fire. `_has_role` reads the private `_roles` SnowflakeList built straight from the payload. The check script asserts `guild.get_role` is reached **zero** times against an empty-cache guild and that `len(member.roles) == 0` there — i.e. it asserts the failure mode is avoided, not just that the happy path works.

**Two of the seven perks are one slot, and the resolution logic breaks its own ladder on purpose.** Higher Reputation XP and Supportive Allies compound — ally happiness already pays +20% XP in the thriving band and Supportive Allies exists to hold allies in that band far longer, so held together they're a sustained **1.30 × 1.20 = 1.56×**. So level 10 does not hand you both. As first built: level 10 switched Higher Reputation **off** and handed the slot to Supportive Allies, and a live Arachnid+ pledge **cancelled that swap** rather than stacking on it, per *"if the user is a server member and hes on patreon and hes got level 10 (which violates the disadvantage) make the patreon perk rule over"*. **The owner then reversed the no-choosing rule after live-testing** — see the copy-pass section below — so the swap became a *default* and `/perks choose` moves between the two. What survived the reversal is the part that mattered: the `rank < LEVEL_5` guard still comes first, so a subscriber outside the server gets **neither** — the pledge *shields* a ladder perk, it never grants one — and exactly one of the two is live in every one of the 18 {no role, L5, L10} × {no pledge, Arachnid, Symbiote} × {no pick, xp, allies} cells.

**Both transitions look like bugs from the player's side, and that is a copy problem rather than a logic one.** Reaching level 10 *lowers* the XP rate of a member who never picks. Letting a pledge lapse moves an unpicked level 10 across to the ally perk. Both are correct, and both happen at a moment the player didn't ask for anything. `/perks status` is the only surface that can say so, which is why it exists at all — every perk here switches with a Discord role, so a player can gain or lose one without any command ever telling them. A member who has actually run `/perks choose` is immune to both, which is the point of storing the pick.

**`/perks` was read-only by construction, and then it wasn't.** The approved plan gave the Bronze camera a **Claim button**; *"no please try and avoid any /perks command which makes the users choose"* killed it, the grant moved to `shop_service.grant_bronze_camera` called from `utils/first_run.py`'s single `before_invoke` slot, and the cog shipped with no button, no select and no stored choice anywhere in it — recorded in its docstring as a *requirement* so the constraint would survive in code rather than only in the conversation. Hours later the owner reversed exactly that requirement for exactly one perk. **The Bronze grant stayed automatic**, which is the part the rule was really protecting; what came back is a choice between two things the player already owns, which is a different thing from a claim. *A constraint written into a docstring is worth writing even when it gets reversed the same day — the reversal was legible as a decision rather than as drift, and it was obvious which half of the rule still applied.*

**Automatic is precisely why the grant still has to return a sentence.** `install_tool` deletes the base camera and returns the scrapped keys specifically so the caller can say so; a silent auto-grant would write off a $150 body with nothing rendered anywhere. Idempotency comes from the inventory rather than a claimed-flag (the perk is checked against a live role), and anyone already on Silver or Gold is **skipped** — granting would demote a $1,000 camera to a free one, the same trade `buy_item` refuses outright. The grant shares the last-active stamp's one-minute throttle, because `grant_bronze_camera` has to query the equipped camera just to learn it has nothing to do and the players it applies to are by definition the most active.

**The dormant `growth_perk_choice` code was *replaced*, not OR'd with the new checks, and that distinction is load-bearing.** A surviving `get_growth_choice` branch beside the new one would apply the same multiplier **twice** to anyone who ever stored a choice, because `ServerPerks._pair` already decides the pair. The function, the column and its migration all stay on disk unreferenced, per the standing note not to delete them; the check script asserts neither `economy.py` nor `ally_service.py` mentions it any more. **And when a stored choice came back later the same day, this column still couldn't serve it** — a `patreon_links` row only exists for someone who has linked a Patreon account, and the pick belongs to any level 10 member of the server, pledge or not. The new one stores the same two strings on purpose, so folding one into the other later needs no backfill.

**Lower Cooldown is scoped by constant name rather than by call site**, so the rule stays self-checking as code moves: apply `scaled()` only where the value written is `PATROL_/BUGLE_/SHAKEDOWN_COOLDOWN_SECONDS`. Six exclusions are asserted **absent** by source scan, and the sharpest is the victim's `TARGET_PROTECTION_SECONDS` — scaling that would let one account's level reach into **another player's** protection window. The attacker's own cooldown is the perk; the defender's shield is not the attacker's to shorten. This perk was also *reversed* out of §9.5's "explicitly cut, do not re-propose" list, and the original worry (a spammy core loop) is answered by scope rather than by shrinking the cut.

**A DB cache was rejected rather than merely passed over.** Ally happiness decay is integrated over the whole elapsed window, so a cached flag would apply Supportive Allies to hours the player spent outside the server — wrong, not imprecise, and against the requirement the whole feature rests on. So `resolve_perks` is called once per command and threaded down, exactly as `tier_rank` already is; `ServerPerks` is frozen because `PatrolBattleView` stores it at construction and a battle started in the guild must finish in the guild.

**The one bug this round found in itself, and it was found by writing the docs rather than by any check.** Organic Webbing is the only perk on this track that also exists on the paid one — and `patrol_cog._webbing_note()` trails a **tier badge**. So a booster with no pledge was credited to **Arachnid**, a paid tier they don't hold: the same misattribution class as the 2026-08-22 bug where a Symbiote subscriber was told a lower tier's perk saved them the vial. Nothing would have caught it, because the *effect* is genuinely identical from both routes and every behavioural assertion passed. Fixed with `PatrolStart.webbing_from_server_perk` (naming-only) and a badge swap; the pledge wins when both apply, since one line can only name one source and the paid grade is the one with a promise attached. *When a perk becomes reachable from a second source, the behaviour is the easy half. Go read what the card now claims about **why** it fired.*

**`check_camera_gold.py` was the only regression, and its old assertion was the wrong one all along.** Inserting `camera_bronze` at index 1 of `CAMERA_FAMILY_KEYS` broke a hardcoded exact-list check. Every consumer reads that list via `.index()` or a slice, so an *insert* is safe and only a **reorder** is dangerous — a reorder is what would make a $1,000 Silver purchase scrap a $3,000 Gold body. The check now asserts the ordering, which is what it meant to assert.

**Verified:** `scratch/check_server_perks.py`, 17 groups, **126 checks, ALL CHECKS PASSED** — including a real `discord.Member` built from a raw payload dict to prove the no-intent claim empirically rather than by citation, both roles together behaving identically to level-10 alone, all 18 pair cells plus both pledge-lapse directions, the camera reindex and `/shop` invisibility (unpriced → invisible, the same mechanism `web_fluid_vial` uses), revocation leaving the Bronze row intact, and a sweep over every panel state for dangling separators. **Fourteen existing scripts re-run clean** (2026-08-30): the twelve fast ones — `check_camera_gold`, `check_rank_map`, `check_revocation`, `check_symbiote_icons`, `check_ambient_scavenge`, `check_symbiote_repair`, `check_stealth_instrumentation`, `check_vial_shop`, `check_patreon_refresh`, `check_tier_accent`, `check_upgrade_welcome`, `check_help` — plus both slow simulation scripts: **`check_override`, 46 checks, ALL CHECKS PASSED**, and `check_venom_trigger`, whose sweep table is unchanged (it asserts nothing; see the dumper note in the verification section, and note its already-documented bracket-1 finding is not from this round). That matters because `battle_service` gained `BattleState.suit_damage_multiplier` this round — it defaults to `1.0` on every non-patrol path, and the boss numbers coming back identical is what confirms the default actually holds rather than merely being intended. `check_ads.py` and `check_invite.py` fail on the known `pool_size`/`StaticPool` trap (backlog item 9) and have nothing to do with this round.

**The live test passed** — *"okay everything works properly"* — and produced seven copy and UX notes, below. **Still outstanding: the commit** (backlog item 1 proposes a 4-way split). The live bot stays inert regardless: `PERKS_GUILD_ID` unset switches the whole feature off, per the two-bot rule.

### Round nine, second pass — the seven notes from the live test (2026-08-30)

*"okay everything works properly its just some small changes"* … *"everything else is perfect"*. Six were copy or glyph swaps; the fourth reversed a design rule.

**Two sentences got cut for the same reason, and it's a reason worth keeping.** The panel opened with *"Everything below is read off your roles the moment you run a command, so it switches on and off with them — nothing to claim and nothing to choose"*, and the Bronze row ended *"it's handed to you automatically, nothing to claim"*. The owner: *"this is something only devs know, no need for this shi in the actual command."* Both sentences were true, both were things I'd been pleased with, and both describe **the implementation's most interesting property rather than anything the player wants**. *The mechanism you're proudest of is the thing most likely to leak into player-facing copy. "Nothing to claim" only reads as reassuring to someone who knew a claim step was possible.* The check script now asserts neither phrase can come back.

**One embed was doing two jobs and telling one set of players something false.** A member standing in the server with no level roles yet saw the same panel as someone running the command in a DM — which told them they weren't in a server they were standing in. Split on `in_perks_guild()`: outside gets the invite and a **Join the Discord** link button, inside-with-nothing gets *"You haven't unlocked these yet."* Only one of the two situations is fixed by joining, so only one of them gets the invite. The original collapse had a defensible reason — both mean "no roles were readable here" — which is how it survived a design review and a build. *"These two states have the same cause" is not the same claim as "these two states want the same words."*

**`/perks choose` cost bare `/perks`, and that's a Discord constraint, not a design call.** A command with subcommands cannot also be invoked on its own, so the panel is now `/perks status`. `help_cog`'s footer updated with it.

**The pledge became a default instead of a veto, and every existing assertion survived that.** *"tweak it so that at level 10 it chooses the ally perk by default but they get to switch back and forth as they like."* The old precedence rule had a live Arachnid+ pledge *cancel* the level 10 swap; now it decides which half the slot **starts** on, and the member moves freely. Both reasons transferred intact — allies by default so level 10 hands you the new thing, XP for a subscriber because they were already paying for it. All nine original truth-table cells still pass unchanged, so the reversal cost no coverage; the table just grew a third dimension.

**A choice needs somewhere to live, and round nine had been quietly proud of not needing a migration.** `users.perk_pair_choice`, nullable, no `server_default` — NULL is a meaningful third state ("never picked", resolve to the pledge-dependent default), so backfilling either value would lock every current level 10 member out of that default. Alembic `5c2ab7f10de4`. *The round's write-up read "no migration" as a principle held under temptation. It was a property of that design, and it evaporated the moment the design changed.*

**`/perks choose` writes even when the pick matches what's already live.** Not a wasted UPDATE: an unset choice resolves to a default that moves with the pledge, so somebody deliberately confirming the perk they're on is asking for it to **stop moving**. That's the difference between "this is where I landed" and "this is what I want".

**A demoted level 10 stops getting their stored pick, and the row stays put.** `_pair()` returns XP at level 5 without consulting the column at all — the live role is the only source of truth, same contract as every other perk here — so the choice comes back with the role rather than being re-earned.

**Three glyph swaps, all with fallbacks intact.** The lit state box is the project's own `ready` glyph rather than a green unicode tick, the ally half of the pair wears custom gift art, and `utils/item_display.badge` moved to `rare_badge` — so Unstable Web Fluid wears real art everywhere it appears, not just here. `emoji()` returns `None` on a miss and every use keeps its unicode fallback, so all three are still correct on a bot with no application emoji uploaded. `OptionChoice` labels are the one place custom emoji can't render at all, so the two slash-command choices take bare names.

**The join-button assertion passed while testing nothing, twice.** First it walked `view.children` — `StaticView`'s contents live inside a `Container`, which exposes them as `.items`, so the walk found the Container and stopped. Then, fixed to read `view.to_components()`, it collected the header thumbnail's `attachment://locked.png` off the same `url` key and failed the *presence* case for the wrong reason. Both fixed by reading the rendered payload and filtering to `http`. *An assertion that a button exists is worthless unless you have also watched it fail — the first version would have passed a panel with no button on it.*

**`check_help` caught `/perks choose` shipping undocumented, which is the whole reason it builds its list from the live command tree.** It walks `bot.pending_application_commands` and expands every `SlashCommandGroup`, so a new subcommand fails there rather than shipping. Fixed by naming it in `help_cog`'s footer rather than by adding an exemption — it is the only thing on this entire track a player *decides*, and it is invisible from `/perks status` alone unless they already hold both level roles. *The check exists because a hand-written list of commands is a list of the commands you remembered.*

## What each of the six actually did


**#1 — per-key rank map.** `ARACHNID_GATED_ITEM_KEYS` is gone; it's now `GATED_ITEM_MIN_RANK`, a `{item_key: min_rank}` map, with `GATED_ITEM_KEYS` *derived* from it for the callers that only ask "is this gated at all?" — so no second list has to be kept in step. (It lived in `shop_service` when it shipped; round two moved it to `patreon_service`.) `get_tier_rank()` stays the single chokepoint, and ranks are always compared with `>=` / `<`, never `==`, so a Symbiote subscriber automatically clears every Arachnid gate.

**#2 — the refresh loop.** `refresh_stale_links()` in `services/patreon_service.py` drains an oldest-checked-first queue; `SchedulerCog.patreon_tick` runs it every 15 min, 25 links per tick, re-reading anything past a 6-hour staleness window. The governing rule is written at the top of that section: **only a successful read of Patreon's identity endpoint may change a stored tier.** A 500, a 503, a DNS blip, a timeout — all fail open, stamp `last_checked_at`, and leave the tier exactly as it was. Erring this way means a lapsed pledge keeps perks for up to one extra interval, which is the right side to be wrong on.

The one exception is deliberate and necessary: a **4xx on the refresh grant** is Patreon stating the grant is gone, which no retry can fix, so that alone clears the tier (`_DeadLinkError`). Pure fail-open would have reopened the hole through the back door — an unverifiable credential would grant paid perks forever. The row is kept, so one `/patreon link` repairs it.

Two traps were caught before shipping and are both under test. Patreon **rotates the refresh token on every use**, so the new pair is committed *immediately*, before the identity call — otherwise a crash in between leaves a spent token stored and the next cycle declares a *paying* subscriber dead. And a refresh response that omits `refresh_token` retains the old one rather than blanking the field.

`RefreshOutcome` exists because a bare `tier: str | None` return cannot express the difference between "they have no pledge" and "we couldn't tell", and that distinction *is* the fail-open contract. `reached_patreon` is the load-bearing field.

**#3 — the Symbiote's own cost.** `SYMBIOTE_OVERRIDE_CHANCE` in `services/battle_service.py`: the suit takes over and attacks instead — no dodge, no combo banked. Shipped at **0.10, Evade-only**; round four moved it to 0.15 and added the gadget hijack; **round five doubled it again to 0.30**, which is where it stands. The reasoning that originally rejected 0.15 here (−7.87% on a boss, dropping a paying subscriber below the 70–75% an unsubscribed player with a full gadget kit gets) is now **history, not a gate** — 0.30 breaks it by a wide margin and shipped as an instructed decision. Nothing holds a "rejected" slot any more; the four-rate cost table in round five above is the whole record. Two other *shapes* were priced and rejected outright; those reasons are recorded in the comment so they don't get re-proposed.

**#4 — Venom Blast validated.** Simulated in `scratch/combat_sim.py`. Combat is a closed system, so this one *was* answerable by simulation, and the `2x` holds. **Round four left the multiplier alone and changed the trigger instead** (0 → 25% integrity, `scratch/check_venom_trigger.py`) — the `2x` is bound to the player-facing "twice as hard" copy, so it isn't a free knob; *when* the blast fires always was. **Round five changed *who* fires it**: the 25% bar is unchanged, but it now arms a button instead of triggering automatically, so the perk stopped being a death save (see round five above). The `2x` was re-measured under the button and still carries the whole perk — win rate across multipliers ×0–×4 runs **2.27% / 5.70% / 14.08% / 27.13% / 39.06%** at bracket 10, so ×2 → ×3 still nearly doubles it. The span narrowed a lot from the automatic era (~81 points to 36.79), but *"do not treat this constant as safe to nudge"* survives on its own merits.

**#5 — Stealth Mode made measurable, and deliberately NOT declared validated.** This is the honest outcome and it matters: the 20-minute threshold **cannot be settled by simulation**, because it depends entirely on how long real players step away from Discord. There's no ground truth to simulate against. So what shipped is the *measuring apparatus*, not a verdict:

- New `shakedown_attempts` table (migration `a7c41e93b508`). Before it, a stealth-protected attempt returned early and charged nobody — so it left **no trace at all**, not even a `transactions` row, and the perk's real firing rate was unobservable.
- `target_idle_seconds` is stamped on **every** attempt, protected or not. That's the design decision worth keeping: any *future* candidate threshold is scoreable from data already collected, instead of needing a fresh instrumentation deploy per number under consideration.
- The gate was split into a reading (`target_idle_seconds`) plus a pure predicate (`stealth_mode_active`) so the logged value is guaranteed to be the one the gate actually judged, and so the analysis script can replay the real rule rather than a copy of it. **Gate behaviour is unchanged** — every boundary is under test.
- Instrumentation is provably non-load-bearing, not merely intended to be: its own session, a blanket `except`, and tests that deliberately break the insert and assert the player's command still resolves and the cash still moves.
- **The constant was not touched, and the code says so** — `STEALTH_MODE_INACTIVITY_THRESHOLD_SECONDS` carries a `STILL UNVALIDATED` note pointing at the analysis script. **Round four changed nothing about the gate** — only that the refusal is now attributed, so a thief can tell it was somebody else's perk rather than a bug.

**#6 — Biomorphic copy scoped.** The perk advertised 3 rolls; 2 only fire in combat. Copy said "(combat patrols)" rather than the mechanic being changed, since the mechanic was right and the promise wasn't. **Round four reopened this and went the other way**: the owner's actual intent was that the webbing collects things *on the way* to Peter's other commitments, so a **fourth** roll now fires on `/tutoring`, `/ally visit` and `/bugle submit`, and the "(combat patrols)" qualifier was rewritten to scope only the two rolls that really are combat-only. Both passes were correct at the time — round one made the promise match the mechanic, round four made the mechanic match the intent.

## The backlog, in the order it's actually ready to be picked up

0. **Live smoke test on the linked Symbiote pledge.** The one thing offline checks cannot cover, and rounds four through seven are the batches that most need it — together they touch copy on six commands, add a **button** to the boss-fight row, convert a `/shakedown` branch to a V2 panel, **send a new DM**, and depend on the emoji actually being uploaded to the guild. Run `/patreon perks` (no Sonic Dampener, reworded Venom Blast, all four glyphs rendering rather than showing as `:name:`, and the Stealth Mode line carrying its block count if any rows exist), a boss patrol pressing **Evade** and a **gadget** to see both override lines, the same fight down past 25% integrity to watch `Venom Blast — Not Charged` become live and then `— Spent`, buying a Gold camera over a Silver one to see *"Stripped Silver-Grade Camera for parts"* and confirm `/inventory` shows one body, `/workbench status` + `/workbench repair` for the reskin, and `/tutoring` + `/ally visit` + `/bugle submit` for the ambient scavenge (30% each as of round six, so expect to repeat). **Stealth Mode needs two accounts and patience, and now has two surfaces to check**: idle the Symbiote account 20+ minutes, then `/shakedown` it from another. The *thief's* panel should be grey-purple (the **target's** Symbiote accent, not the thief's), thumbnailed, and titled "Stealth Mode" rather than "Parker Luck." The *target* should then receive round seven's **DM** — its own Symbiote accent, the stealth glyph leading, the tier badge trailing, "20+ minutes" in the subtext, and the block count. Two things to know while testing it: the DM is throttled to one per 15 minutes per target, and `/admin bypass` on the **account being shaken down** (not the tester's) defeats that throttle, since the `stealth_dm` cooldown is keyed on the target. Confirm the DM is *absent* on a successful shakedown — that scoping is deliberate (round seven). **`utils/icons.py`'s `EMOJI` is a static dict of hardcoded IDs** — if any of the four were uploaded to a different guild than the bot is in, they render as literal text and every check in `check_symbiote_icons.py` still passes, because that script asserts the *string* is present.

   **Round eight adds two more things to this list, and the second one involves real money.** Run `/lab status`, `/lab brew` and `/lab collect` and confirm each success card shows its own pitch line in the footer subtext plus a **Web-Fluid Stash — from $3** link button *inside* the container rather than floating below it, and that the button actually opens the shop page. Confirm `/lab brew` with a wallet under $30 shows the plain error with **no** button (deliberate — see round eight). Then the fulfilment path end-to-end, which is the one that matters: `/admin inventory give-item` with `user_id` set to the ID of an account **not in the server**, quantity 300, and check the vials land and the receipt names the right person. Try it once with a plausible-but-wrong ID to confirm it refuses rather than minting a profile, and once with both `user` and `user_id` filled to confirm it refuses that too. **Do this before the first real order arrives, not after** — offline checks cover the branches but not whether Discord actually resolves a non-member user for this bot's token.

   **Round nine's block is mostly *done*, and what's left is the part the second pass added.** The first pass was live-tested on 2026-08-30 — *"okay everything works properly"* — which is what closed the biggest offline gap in this file: the whole track rests on a claim about what Discord puts in an interaction payload, and the check script only ever proved it against a *fabricated* payload. A real guild answered it. What has **not** been exercised on a real bot is `/perks choose`, because it didn't exist yet: run it at level 10, confirm the pick sticks across a later `/perks status`, confirm switching back works, and confirm it refuses politely below level 10 and outside the server. Also worth re-running once, since both changed shape: `/perks status` with **no roles while standing in the server** should say "you haven't unlocked these yet" with **no** join button, and the same command in a DM should show the invite **with** one. The rest of the block passed and is kept here only as a re-test script if the code moves: booster-only → `/patrol` consumes no vial and the note trails the **booster** badge rather than Arachnid's; level 10 → the Bronze camera arrives automatically on the next command with a line saying what it scrapped, `/inventory` shows the base body gone and exactly one equipped, and removing the role reports locked-with-base-stats while the row survives; `/lab brew` quotes **3 minutes**; patrol/bugle/shakedown come back a quarter sooner while `/daily` is untouched. And the pair, still the part most likely to look broken: **if the note under those two rows doesn't explain why one of them is dark, the copy is the bug, not the logic.**

1. **Commit round nine.** 24 modified code files plus 3 new ones plus both docs, verified and live-tested but uncommitted — the community-server perk track. Worth splitting five ways: the perk engine and its threading (`server_perks.py`, the six services, `config.py`, `first_run.py`), the Bronze camera (`items.json`, `shop_service`, `patrol_service`'s ladder), the `/perks` panel (`perks_cog.py`, `help_cog`, `icons.py`, `bot.py`), the webbing-attribution fix (`patrol_cog`, `patrol_service`), and the second pass (`perks_cog`, `server_perks`, `db/models.py`, the migration, `item_display.py`). The webbing one is a **bug fix to already-shipped code** and deserves its own message rather than being buried in a feature batch; the second pass carries the only migration and is the only part not yet exercised on a real bot. ~~Commit round eight~~ — **done**, `d91a092`. ~~Commit rounds two through seven~~ — **done**, landed across `4c8a4b5..b88af46` and pushed.
2. **GAME_DESIGN.md §9 doesn't document the tier accent.** The perk exists in code and is visible to every subscriber, but the spec that lists what each tier gets has no mention of it — grep for `A91D3A` in the docs returns nothing. This is the exact drift CURRENT.md is meant to catch: the next person pricing the tiers won't know the accent is part of what's being sold. Small write-up, and the two hex values belong there rather than only in `patreon_service`. (Round four's doc pass covered §6.2, §9, §9.3, §9.5, §12, §13, §14, §15 and §16; round five went back over §6.2, §9.3 items 1 and 4, and the shop rules and item catalog. The accent is what neither reached.)
3. ~~**The 4 Server Booster perks** — Higher Suit Integrity, Higher Reputation XP, Supportive Allies, Quicker Web Brewing~~ — **shipped in round nine, at the locked §9.5 numbers (0.30 / 1.30 / 0.70 / 3 min), plus three more the design doc never speced.** Not "Booster" any more: only Organic Webbing is boost-gated, and the other six hang off level-5 / level-10 roles. **The "blocked on the owner" note under this item was wrong for nine days** — both blockers below are now closed, and neither needed the owner in the end. Supportive Allies' *hours* moved with the 24h retune (full drain 32–37h now); its locked 25–35% band did not, and 0.70 sits in it.
4. **`.env.example` is missing `PATREON_ARACHNID_TIER_NAME` / `PATREON_SYMBIOTE_TIER_NAME`, with no startup validation.** If either is unset, every subscriber silently resolves to rank 0 — every paid perk switches off with no error anywhere. Both *are* set in the local `.env` (`Arachnid` / `Symbiote`), so nothing is broken today; this is a deploy-time landmine. Small fix: add them to `.env.example` and fail loudly at startup if unset. **Every round since has raised the stakes**: a misconfigured tier name now silently switches off *owned* gear (reads as "my Gold camera stopped working" rather than a config error), silently drops every subscriber's accent — indistinguishable from a free account, so nobody would report it — and after round four also silently reverts the repair reskin and stops every ambient scavenge, all of which read as "the bot changed" rather than as a config error.
5. **`alembic upgrade head` from an empty DB is broken** (pre-existing, not from this session — confirmed empirically by reproducing it at the *old* head `df532d94924d`). `alembic/versions/e83fd19455a5_baseline.py` calls `Base.metadata.create_all()` against *current* models, so a fresh DB gets every modern column and later `op.add_column` migrations collide — it dies at `44f8f54cbf0b` with `duplicate column name: boss_clears`. Workaround for a fresh DB: `create_all` then `alembic stamp head`. A real fix means freezing the baseline to the Aug 2026 schema instead of tracking `db/models.py`. Documented in GAME_DESIGN.md §20.13.
6. **`wipe_user` doesn't clean `PatreonLink` or `AdminUser` rows.** `ShakedownAttempt` is deliberately excluded — it's a log, not state, and the `Transaction` precedent argues for leaving it. Noted in §20.12.
7. **`camera_break_chance` in `data/loot_tables.json` is dead config.** Three per-tier values (0.08 / 0.14 / 0.2) that **no Python code reads** — verified by grep across the tree. `battle_service` computes camera breaks from its own `0.06 + 0.05 * hits_taken` formula instead. Harmless today, but it's a live trap: anyone tuning camera-break rates would edit the data file, see no change, and have nothing to tell them why. Either wire the formula to read it or delete the three keys — the choice matters (per-tier data would be a mild behaviour change).
8. **`inventory_service.add_item` doesn't validate `item_key`.** A typo'd key creates a real inventory row for an item that doesn't exist in `data/items.json`, and nothing errors. Round four's ambient scavenge is the newest caller and passes keys from a module-level table, so it's safe by construction — but the reason to know this is that a *future* caller passing a computed key gets no protection. A `session.get(Item, key)` guard would cost one primary-key lookup on a path that already writes.
9. **`db/base.py` passes `pool_size`/`max_overflow` to `create_async_engine` unconditionally, which crashes on any `:memory:` URL.** From `e76d8da` (2026-08-27). SQLAlchemy resolves the pool class from the URL — a file URL gets `AsyncAdaptedQueuePool` (accepts them), `":memory:"` gets `StaticPool` (rejects them, `TypeError` at engine construction). **The live bot is unaffected**, since it runs on a file DB; the blast radius is anything pointed at an in-memory DB. Fix is one guard — skip both kwargs when the resolved pool is `StaticPool`, or gate them on the URL not being `:memory:`. Left alone in rounds eight and nine on purpose: out of scope for both batches, and it belongs to whoever owns the pool sizing, whose comment block explains the two numbers in detail. **Round nine found round eight's account of this incomplete, which is why it's still worth reading rather than just closing.** Round eight said the two scripts it fixed "were the only ones using `:memory:`" — they weren't. **`check_ads.py:17` and `check_invite.py:18` still set it and still die at `import db.base`**, before a line of their own code runs, and both were failing this way for three days without being noticed because nothing routine runs them. Either apply the one-line guard and revive both, or switch them to a throwaway file path like the other 23. Until then, treat a red result from either as this trap rather than as a real failure — and note that **`check_invite` is the only coverage the `/invite` command has**, so its silence is a coverage gap and not just a broken script.
10. **`cogs/status_cog.py` and `cogs/vote_cog.py` exist but are not in `bot.py`'s `EXTENSIONS`** — 26 `*_cog.py` files, 24 loaded as of round nine (noticed 2026-08-28 while correcting the extension count above; **still not investigated**). This may well be deliberate — the status rotation looks like it moved into `scheduler_cog` (`487f06c` "fix status rotation bug"), and `scheduler_cog.py:50` still refers to a note in `status_cog.py` as if it were live. What makes it worth a line rather than a shrug: **`scratch/check_topgg_votes.py` imports `_topgg_vote_handler` from `vote_cog` and presumably passes**, so there is a green check script covering a cog the bot never loads. That's this project's documented failure mode in its purest form — a check that describes code rather than the product. Confirm each is intentionally shelved (and say so in a comment), or wire it back in.

11. **`scratch/check_venom_trigger.py` is named like a check and asserts nothing** — 0 `check()` calls, no exit guard, `rc=0` whatever it prints (found 2026-08-30 while re-running it for round nine). It's a *measurement sweep*, and a good one; the problem is purely that its name and its old table row claimed coverage that lives in `check_symbiote_icons`. Two honest fixes, and they are not equivalent: **rename it** (`sim_venom_trigger.py`, matching `sim_ad_rate.py` and `combat_sim.py`, which are already named as explorations) so a sweep of `check_*` exit codes stops silently counting it as a pass, **or** give it the one assertion it's already computing — *criterion 1 of the balance gate*, that the shipped bar clears the no-Patreon floor. The second is more valuable and slightly awkward: the floor check **currently fails at bracket 1** by design (§9.2, −0.30% to −0.38% depending on seed), so the assertion has to encode "negative at b1 only, positive at b5/b10/b20" rather than a flat threshold. That's more work than a rename but it's the version that would actually catch a regression. Same question applies to `combat_sim.py`, which reports FINDINGs and also exits 0 — the difference is that one is *named* as a simulation and this one isn't.

## Open — needs the owner, not an implementation decision

- **Lapse-notification copy — now the only silent direction.** Round three gave *upgrades* a DM (`patreon_tier_upgraded` → the welcome card), so the loop is no longer symmetrical: a tier going **up** notifies, a tier going **down** only logs at `warning` level (§9.1.1). That asymmetry is defensible — a grant is good news in copy that already exists, while `NO_PLEDGE_INTRO` reads as a greeting and lapse copy is its own tone question — but it's now a *choice* rather than an omission, and it's the loose end in both batches. The inline `Inactive` labels are the current substitute and they're honest, but they're **pull, not push**: the player has to open `/inventory` or `/gadget status` to find out. Nothing is broken. This is a decision about whether a lapse deserves a notification, and if so what it says.
- ~~**The Discord Members intent.**~~ **Closed 2026-08-30 — the premise was false.** This said rebuilding the Booster track "requires re-enabling it to read boost status: a live-bot config change, not just code." It doesn't. Discord ships the invoker's role list *and* `premium_since` inside every interaction payload, and pycord stores both verbatim: `discord/member.py:349` (`self._roles = utils.SnowflakeList(map(int, data["roles"]))`) and `:346` (`self.premium_since = utils.parse_time(...)`). The bot still runs `Intents.default()` and round nine reads both without touching the gateway. The revert-then-check-in concern is also settled: the owner asked for the rebuild directly, and it's built (§22). **Note that this item is *not* the same as the separate `server_invite_reward` deferral, which needs to enumerate members rather than read the invoker — that one is still genuinely intent-gated and stays deferred.**
- ~~**Quicker Web Brewing's track.**~~ **Settled by instruction, 2026-08-30**: the owner assigned it to **community-server level 10**, so neither "ungated/everyone" nor "Booster" — the 2026-08-21 grouping is superseded rather than confirmed.
- **The four `PERKS_*` values, and this is the only thing between round nine and live.** `PERKS_GUILD_ID`, `PERKS_BOOSTER_ROLE_ID`, `PERKS_LEVEL_5_ROLE_ID`, `PERKS_LEVEL_10_ROLE_ID` all default to `None`, and an unset `PERKS_GUILD_ID` switches the entire feature off — deliberately, per the two-bot rule that anything the live bot needs must default to off in code. So this merges inert and the owner fills in `.env` when ready. Two things worth deciding rather than discovering: **whichever leveling bot grants the level roles has to actually grant a role** (round nine can only read roles, not a bot's own XP store), and if it *removes* the level-5 role when granting level-10 that's fine — `_pair()`'s `rank == LEVEL_5` test is correct either way, and both-roles-at-once is asserted identical to level-10-alone.
- **Three emoji are not uploaded yet: `booster`, `server_level_5`, `server_level_10`.** `emoji()` returns `None` for a missing key, so every surface renders correctly *unbadged* — the perk lines are still true, they just don't say where the perk came from. Not a blocker, and asserted both ways (the checks cover the badged and unbadged renders). Worth knowing because it's the one respect in which round nine's copy is incomplete on purpose, and because `utils/icons.py`'s `EMOJI` is a static dict of hardcoded IDs, so an upload to the wrong guild renders as literal `:booster:` text with every check still passing.

## Awaiting data, not work

- **Stealth Mode's 20-minute threshold.** The apparatus is shipped and the constant is honestly labelled unvalidated. Once real `/shakedown` traffic accumulates, run `python scratch/analyze_stealth_mode.py` — it reports the firing rate against Symbiote targets (the honest denominator), an idle-time distribution, and a counterfactual table for every candidate threshold. It refuses to conclude from an empty table, and it flags small samples as directional only. Target band: a threshold that protects a clear *minority* of attempts — high enough that actively-playing subscribers stay targetable (otherwise it's the pay-to-win immunity that was already rejected), low enough that it fires often enough to be noticed.
- **That table now has two player-facing consumers, and it changes the stakes of moving the constant.** Round six put a per-subscriber block count from these same rows on `/patreon perks`, and round seven put the same count in a DM — so the number isn't just player-visible, it gets *pushed*. Raising the threshold makes existing counts stop growing; lowering it makes them grow faster — and the count is cumulative, so a retune can't be reasoned about purely as "how often does it fire from now on". Nothing needs doing about that, but decide it deliberately rather than discovering it.
- **What that data will *not* contain**, so it isn't mistaken for a complete census of intent: cog-level refusals never reach the resolver, and the 2-minute `shakedown_target` cooldown masks a second attempt on the same target.

## Numbers still not simulation-verified

- **Spider Bots / Electric Webbing proc chances.** Retuned in `ad01949` against the existing 0.25–0.55 gadget baseline, not by a binary-search pass.
- **The ambient scavenge's `0.30` and its 3:1 item weighting** (round four at 0.20, raised in round six). Both are reasoned rather than measured. The weighting favours the $80 Spandex Fabric over the $150 Micro-Electronics because repairs need spandex every time and electronics only past the 50-point threshold — that part is sound and unchanged. The rate is now the perk's highest, which round six argues for on the grounds that the two 0.20 rolls carry extra preconditions this one doesn't. **What's still unmeasured is the same thing it was at 0.20**: how much the component drip offsets §16's repair costs over a real play session. Two facts make that bound tighter than the raw EV suggests, and both are worth carrying into any future tuning — a repair consumes exactly **one** spandex regardless of how much integrity is missing, and there is **no NPC buyback anywhere in the game**, so a surplus component can only reach another player via `market_service`. The drip therefore buys repair self-sufficiency and market supply, not income. Nothing has measured either.
- **The override's measured cost is an over-estimate, and knowingly so.** `combat_sim` models gadget presses synthetically and has **no fumble rate**, so the boss figures (−15.91% at 0.15, −29.02% at 0.30) are a ceiling, not an estimate. The visibility numbers are exact — 1.20 hijacks per fight and 78.8% of fights at 0.30 come from the real constant rolled by the real resolvers.
- **Every Venom Blast figure from round five onward is a *floor*, for the opposite reason.** `run_fight` presses the blast the round it arms, which is the greediest policy available; a player who holds the charge for a worse moment reaches strictly better outcomes. So the sim under-reports the button and over-reports the override, and the two errors do **not** cancel — they're measured on different presses.

Venom Blast's `2x` came off this list in `49ad4f9`; its **trigger threshold** came off it in round four (`check_venom_trigger`, a 7-point sweep at four seeds), and round five re-measured the same bar under the button rather than taking round four's numbers forward. The crime source/sink balance came off it in `c91f4fb`.

## Verification pattern for this project — don't skip it

Run in this order. Everything below was re-run clean at the end of round nine except the two `:memory:` scripts in backlog item 9.

```bash
python -m compileall -q cogs services db utils alembic
```

Then load every extension end-to-end (**24** of them as of round nine, which added `perks_cog` — `v2_demo_cog` was deleted in round three, and note that `cogs/status_cog.py` and `cogs/vote_cog.py` exist on disk but are *not* in `EXTENSIONS`, so 24 of 26 cog files load; see backlog item 10) using the project venv — `.venv/Scripts/python.exe`, **not** bare `python`, which has no `discord` module:

```bash
./.venv/Scripts/python.exe -c "import discord, bot; b = discord.Bot(intents=discord.Intents.default()); [b.load_extension(e) for e in bot.EXTENSIONS]; print(f'{len(bot.EXTENSIONS)} loaded')"
```

**A loaded extension is not a registered command, and the accessor matters.** `bot.walk_application_commands()` returns **nothing** before a sync, so a smoke test built on it reports `0 application commands` and looks like a catastrophe when everything is fine. `bot.pending_application_commands` is the one to read: **22** top-level commands as of round nine, and it exposes each option's real `input_type` as Discord will receive it. That's worth more than a source read for anything schema-shaped — it's how round eight confirmed `give-item`'s `user_id` is genuinely a `string` option and not an `integer` (§19.7's snowflake-precision hazard), rather than trusting that `Option(str)` in the source means what it looks like. **`/perks` is round nine's addition and it is a *group*, so the count is unchanged by its second subcommand** — walk `cmd.walk_commands()` on a `SlashCommandGroup` to see `perks status` / `perks choose`, which is exactly what `check_help` does and how it caught `/perks choose` shipping undocumented.

**Neither of those two steps catches a shadowed import, and round five hit that twice.** Writing `scrap_note = ""` as a local in a function that also does `from services.shop_service import scrap_note` compiles fine, imports fine, loads every extension fine (23 of them at the time), and then raises `TypeError: 'str' object is not callable` the first time the branch runs. It happened once in `shop_service.buy_item` (caught by a check script, because that path has one) and once in `cogs/admin_cog.py`'s give-item branch (caught only by reading the file, because that path has none). The locals are named `scrapped_note` in both places now. **The general shape to watch for: a local whose name is the obvious one is often the imported helper's name too, and every cheap verification step is blind to it.**

Then the functional checks in `scratch/` (gitignored, so they live only in the working copy). Each builds a throwaway DB, drives the real service functions, and exits non-zero on failure — **except two that assert nothing at all, one of which is in this table; see the dumper note below.** **`scratch/` holds 25 `check_*.py` scripts** as of 2026-08-30; the fourteen below are the ones covering this session's rounds (the rest predate it), and all fourteen were re-run clean on 2026-08-30. **Two scripts outside this table, `check_ads.py` and `check_invite.py`, currently fail at `import db.base` for a reason unrelated to any round here** (backlog item 9).

| Script | Covers |
|---|---|
| `check_rank_map` | the per-key gate map (32 checks) |
| `check_camera_gold` | the camera ladder, the admin-grant path, and **round five's delete-and-say-so** (the receipt text is asserted, not just the row count) |
| `check_override` | the Symbiote override — updated in round four for the gadget hijack, and again in round five for **0.30** |
| `check_patreon_refresh` | the refresh loop (74 checks) |
| `check_stealth_instrumentation` | the shakedown log, round six's count, **and round seven's DM + its 15-minute throttle** (105 checks) |
| `check_revocation` | lapse + unlink at use time (51 checks) |
| `check_tier_accent` | `make_container`'s accent — **first run in round seven**, 31 checks, passing |
| `check_upgrade_welcome` | the pending-link fast lane + the upgrade DM (17 checks) |
| `check_venom_trigger` | the trigger sweep, re-pointed in round five at the **button** — 7 bars × 4 brackets × 4 seeds. **A measurement script, not an asserting one: zero `check()` calls, no exit guard** — see the dumper note below. The blast's three labels, its once-per-fight charge and the override exemption are asserted in `check_symbiote_icons`, not here. Slow |
| `check_ambient_scavenge` | round four's Biomorphic scavenge across all three activities — **and the one place the 0.30 rate is asserted as an exact value**, so a future move has to pick a new band here |
| `check_symbiote_repair` | round four's reskin, proving mechanics are byte-identical at all three ranks |
| `check_symbiote_icons` | round four's two-glyph attribution rule at every render site, plus round six's Stealth Mode panel conversion (144 checks) |
| `check_vial_shop` | **round eight** — the vial bundle table, `StaticView`'s `link_button`, all five `/lab` surfaces (three success cards + both refusal branches, including the wallet-short one), that the refusals keep the shared error identity and colour rather than a tier accent, the price-free button label, `_resolve_target`'s raw-ID branches, and that the copy tracks its constants (it moves them and re-renders). 156 checks. |
| `check_server_perks` | **round nine** — 17 groups, **126 checks**. The no-intent claim proved against a real `discord.Member` built from a raw payload; the guild gate in a DM and in a foreign guild, and `in_perks_guild` separately over in-guild / DM / foreign / feature-off; cache-independence asserted as `guild.get_role` reached **zero** times; the ladder, with both level roles behaving identically to level-10 alone; the exclusive pair over all 18 {role} × {pledge} × {stored pick} cells plus both pledge-lapse directions, a garbage stored value falling back to the default, and `resolve_perks` reading the column at level 10 but *not* at level 5 while the row survives the demotion; the camera reindex, `/shop` invisibility and revocation-with-row-intact; the cooldown scope, with six exclusions asserted **absent** by source scan; that neither `economy.py` nor `ally_service.py` still references `get_growth_choice`; the webbing attribution truth table; every perk number moved-and-re-rendered per §9.3; and the second pass's copy rules — both cut sentences unable to return, `ON` being `emoji("ready")` with no `✅` left in the panel, the custom gift glyph, `rare_badge` on Unstable Web Fluid and nothing on an ordinary item, and the join button present on the out-of-server panel and **absent** on the nothing-unlocked one. |

**Two of these are slow enough to matter**: `check_venom_trigger` and `check_override` each run hundreds of thousands of simulated fights and take minutes, not seconds. Run them in the background rather than assuming they hung. The other twelve finish in seconds.

**Round nine's regression was in `check_camera_gold`, and its old assertion had been the wrong one all along.** Inserting `camera_bronze` at index 1 of `CAMERA_FAMILY_KEYS` broke a check that compared the list against a hardcoded exact set. Nothing about the *product* broke: every consumer reads that list through `.index()` or a slice, so an **insert** is safe and only a **reorder** is dangerous — a reorder is what would make a $1,000 Silver purchase scrap a $3,000 Gold body. So the check now asserts the *ordering* (`index(silver) < index(gold)`, and that both sit above bronze), which is the invariant it always meant to protect. **That's the third variant of the check-script lesson in this file: not a check that failed correctly (round four), nor one that passed while asserting the wrong design (round five), but one that failed while asserting something stricter than the contract.** An exact-list assertion over a list whose *order* is the contract will fire on every safe change and teach nothing when it does.

**A check script encodes the contract as of the day it was written, and round four proved that cuts both ways.** `check_override` *correctly failed* after the batch — it asserted `rate is 0.10`, `rate stays under 0.15`, and `SYMBIOTE_OVERRIDE_CHANCE is read in exactly one place`, all of which were true on 2026-08-23 and all of which the owner's decisions deliberately overturned. That's the script doing its job. The fix is to update it *and record why the old bar no longer binds* — the −7.87% that rejected 0.15 was measured against a configuration with the Sonic Dampener still in it. **A stale check that's silently relaxed to `>=` teaches nothing; one that's re-pointed at the new number with the reasoning attached is the whole value.** The "read in exactly one place" check is now "exactly two", kept as an exact count rather than `>= 1` so a third override surface can't be added silently — Attack must never become one.

**Round five is the other half of that lesson, and the more dangerous one.** `check_camera_gold` *passed* — and the bug was still shipped, because the assertion was `Silver ends up unequipped` and the design it encoded was itself wrong. A failing check argues with you. A passing one that asserts the wrong thing agrees with you, which is worse. There's no general defence, only a habit: **when a report and a green check disagree, the check is describing the code and the report is describing the product.** Go look at the surface the reporter was looking at — here, `/inventory`, where an unequipped $1,000 camera rendered with no annotation at all. The assertion is now `Silver is deleted, not kept unequipped`, and the ones next to it check the *receipt text*, because the surface is the thing that was broken.

**Round eight adds a third variant: an assertion that was wrong on its own terms, in a script that was failing.** `check_vial_shop` reported *"brew-fail pitch dropped the old price"* — and the copy had not dropped it. `"$3" in "no $30"` is `True`, so the substring test matched the price it was looking for *inside* a larger number. A failing check normally earns trust, which is exactly what made this one expensive. It's fixed at both levels: a `quotes_price()` helper with a `(?![\d.])` lookahead now backs six assertions, and the copy stopped quoting `BREW_COST` besides. **Every naive `in` test against a number is this bug waiting for a longer number to exist** — and prices are the worst case, since `$3`, `$30`, `$300` are all real values in this feature.

**Two scripts named `check_*` assert nothing at all, and round nine found that one of them is *in the table above*.** `check_admin_info` was the known case — it prints the rendered container for eyeball inspection, has no `check()` calls, and *deliberately* reads the live dev DB rather than a throwaway (its header explains why: the thing it renders is a live user count). The one that had been mis-filed is **`check_venom_trigger`: zero `check()` calls, no `sys.exit` guard, `rc=0` unconditionally.** It is a *sweep*, and its own docstring says so — it "asks" whether a higher bar is simply better and whether the shipped bar clears the no-Patreon floor. This table used to credit it with asserting the blast's three labels, the once-per-fight charge and the override exemption; **those live in `check_symbiote_icons`**, which does assert them. Nothing was ever uncovered — but the coverage claim was pointing at the wrong file, which is the same drift this document exists to catch. **A green `rc=0` from either script means "it didn't crash", nothing more.** The other 13 in the table all signal failure properly, by two different routes worth knowing apart: `sys.exit(1 if failures else 0)` in most, and a `raise AssertionError(f"FAILED: {label}")` in `check_tier_accent` and `check_upgrade_welcome` — which still exits non-zero, but stops at the *first* failure instead of reporting all of them.

**And the sweep prints a real finding that nothing enforces, which is the point of knowing it's a dumper.** Re-run 2026-08-30: *"bracket 1: 97.05% vs 97.34% (−0.30%) **BELOW THE FLOOR**"* — the shipped 25% bar leaves the tier a hair *net negative* against an unsubscribed account at the first boss. This is **already documented and deliberate** (GAME_DESIGN.md §9.2 records −0.38% from `combat_sim`'s own seeds, with the reasoning: a comeback perk has no headroom at a boss you already win 97 times in 100, while the override still charges its ~0.3 points). The two numbers differ only by seed. So it is not a new bug — but it is criterion 1 of the balance gate printing a failure at `rc=0`, and the only thing standing between that and a silent regression is somebody reading the output.


`check_revocation` is the round-two one. It covers both lapse and unlink for gadgets *and* cameras, gated-vs-free isolation, a locked gadget surviving a certain-to-fire break roll, the upgrade refusal taking no money, resubscription restoring both, Gold demoting to base stats while the Gold row stays owned, and both badge helpers.

`check_upgrade_welcome` is round three's. It drives `refresh_stale_links` over a 7-row fixture spanning both staleness windows, asserts the exact selection set, asserts that a fast-lane row always sorts *behind* every six-hour-stale subscriber (the anti-starvation claim in that function's docstring), and then dispatches `patreon_tier_upgraded` on a real `discord.Bot` with the real cog to prove the listener is wired and that a raising `_send_welcome` cannot escape into the tick.

`check_tier_accent` was written in round three and **left unrun** — the owner confirmed the accents working live in Discord and called the offline check unnecessary ("all the accents work perfectly"). Round seven ran it for the first time, because that batch touches `make_container`'s accent on a *third* surface: **31 checks, all passing.** Round eight ran it again (it's the only script covering `utils/v2_embeds.py`, which that batch modified) — still 31, still passing. Treat its result as known-good as of 2026-08-28 rather than unknown, and keep running it — two of the last two accent bugs were on `/shakedown` and neither would have looked broken on screen.

**Round eight found why it and `check_help` had started failing, and it was neither script's fault nor the batch's.** Both died on `import db.base` with `TypeError: Invalid argument(s) 'pool_size','max_overflow' sent to create_engine(), using configuration SQLiteDialect_aiosqlite/StaticPool/Engine` — before a line of their own code ran. Cause: commit `e76d8da` ("fix database lock", 2026-08-27) passes `pool_size`/`max_overflow` to `create_async_engine` unconditionally, and **SQLAlchemy picks the pool class from the URL** — a file URL gets `AsyncAdaptedQueuePool`, which accepts them, while `":memory:"` gets `StaticPool`, which does not. Those two scripts were **not** the only ones using `:memory:`, which round eight asserted and round nine disproved: `check_ads.py` and `check_invite.py` still do, and still die the same way — see backlog item 9. `check_help` and `check_tier_accent` now use a throwaway file path like every other script here. **The live bot is unaffected** (it runs on a file DB), so this is a latent trap rather than a bug in production — but `SPIDEY_DB_URL=":memory:"` crashes the bot at startup today, and a one-line guard in `db/base.py` (skip the two kwargs when the resolved pool is `StaticPool`) would close it. Left unfixed deliberately in both rounds: it's outside their scope and belongs to whoever owns the pool sizing.

**Two traps specific to that script, both of which produced false passes or wrong failures first:**

- **`os.environ.pop("PATREON_CLIENT_ID")` does not make a script offline.** `config.py` calls `load_dotenv()` at import, which copies `.env` into the environment *after* the pop. The first run therefore used the real credentials and fired four requests at Patreon with the fixture's junk tokens (rejected; only the throwaway DB was written). Blank the **module attributes** instead — `config.PATREON_CLIENT_ID = None` after `import config` — since `patreon_service` reads them at call time.
- **pycord keeps cog listeners in `Client._event_handlers`, not `bot.extra_events`.** The discord.py name doesn't exist here; `client.py`'s `dispatch` reads `_event_handlers.get("on_<event>")`. Private, but worth asserting separately from the dispatch itself so a failure distinguishes "never registered" from "registered and never ran".

**A source-grep assertion must strip comment lines.** Three of `check_revocation`'s checks failed on the first run for a reason worth remembering: they asserted that `"Swinging-Grade"`, `"Battle-Grade"` and `"silver lens"` no longer appear — and the comments explaining *why those labels were removed* naturally quote them. All four hits were comments. The script now filters lines starting with `#` before searching. Verifying a string's absence is the case where this bites; presence checks are unaffected.

**It bit again in round four, which is what makes it a pattern rather than an anecdote.** `check_symbiote_icons` asserts that `PatrolBattleView._render_final` doesn't hardcode a tier name — and that function's comments say "this used to hardcode Arachnid" and "told a Symbiote subscriber on a Gold body". The failure was in the *test*, not the code. It now shares a `_code_only()` helper whose docstring states the reason: matching comments makes the assertion unfalsifiable in either direction. **Anything asserting the absence of a string in Python source needs this from the start**, because the comment explaining a removal is exactly where the removed string lives on.

**Two more round-four traps in scratch scripts, both cheap to hit again:**

- **`-# ` breaks a naive "the glyph leads the line" check.** Discord's subtext marker has to come first or the line renders full-size, so `line.startswith(glyph)` fails on every subtext line even when the glyph is correctly placed. Strip the marker before asserting — it's markup, not content.
- **pycord's decorator replaces a cog method with a `SlashCommand` object**, so `inspect.getsource(SomeCog.some_command)` raises `TypeError: module, class, method, function... was expected`. Use `.callback`.

**And one about `combat_sim.py` itself, which is an exploration script rather than a check script (it exits 0 even with FINDINGS).** Round five left it reporting **six** findings where only one was about the shipped configuration: the floor check looped over every threshold in its own sweep, so a bar of 0% — which the button can never arm at, i.e. Symbiote paying the override's cost for a perk that never fires — of course sat under the free floor and got flagged. Five counterfactuals the script synthesized itself buried the one finding that meant something. It also blamed Venom Blast for a delta that was the whole *tier* toggled off vs on, override included, which would have sent the next reader to tune the wrong constant. Both are now fixed and it reports exactly 2, both real. **A FINDING has to mean "go look" — a sweep table is where counterfactuals belong, and a delta's label has to name what was actually toggled.**

**Every scratch script must set `SPIDEY_DB_URL` before anything imports `db.base`.** `config.py` reads `SPIDEY_DB_URL` — there is no `DATABASE_URL` — and it defaults to `./spidey.db`, so a script that sets the wrong name silently runs against the live dev database. Each script carries this as a comment.

Two more traps that cost time this session, both in scratch scripts rather than the app: **`User.reputation_level` is a read-only property derived from XP** — seeding a test user needs `reputation_xp=500_000`, and passing `reputation_level` raises `AttributeError: property ... has no setter`. And Python's `tempfile.gettempdir()` **is** safe for throwaway DBs despite the Git Bash `/tmp` gotcha below, because it returns a native Windows path; it's the *shell-provided* `/tmp/...` string that sqlite can't open.

Two Windows gotchas, both still true: **sqlite can't open a Git Bash `/tmp/...` path** (keep throwaway DBs inside the project dir), and a scratch script run from `/tmp` won't find the `services` package for the same reason. Standalone scripts need `sys.path.insert(0, '.')` since the repo root isn't installed. Also, the console can't print em-dashes or custom emoji — the `?` you see is a display artifact, not a data bug, but it **does** corrupt `Edit` `old_string` matching, so never build one from console output on this platform.

One coupling worth knowing before touching `db/base.py`: `async_sessionmaker(engine, expire_on_commit=False)` is **load-bearing** for `refresh_stale_links`, which commits per-link inside a loop over ORM objects. With expiry on, reading the next link's `access_token` would need implicit IO and raise `MissingGreenlet` under asyncio. The comment in `refresh_stale_links` says what to do if that setting ever changes.
